# Tasks: tidepool — improving the LFM2.5 on-device family for our agent stack

Status: `[ ]` todo · `[~]` in progress · `[!]` blocked · `[x]` done · `[-]` cancelled (the operator closed the project)
Active stage: s5

> Project notes (from the operator's plan in `documents/liquid-primus/`): the goal is
> improved LFM2.5 checkpoints plus a hybrid MoE family, specialized to this
> organization's stack, across four on-disk device tiers, with 4-bit GGUF forms that hold
> the full-precision quality bar. Priority when goals conflict: tool-calling and agentic
> reliability first, then on-device efficiency, then no regression in general quality.
> Reference device is a single 8 GB consumer GPU. The operator wants artifacts mirrored
> to their GitHub and Hugging Face repos as they land.

## s0 - Intake & scoping
- [x] s0.1 Capture initial prompt → initial-prompt.md
- [x] s0.2 Self-scope → provisional overview.md + assumptions

## s1 - Literature review
- [x] s1.1 Formulate research queries
- [x] s1.2 Run literature searches (academic-research CLI); copy papers → stages/s1-literature-review/papers/

## s2 - Research summary
- [x] s2.1 Extract relevant learnings per paper (link back to papers/<id>.md)
- [x] s2.2 Synthesize focused research summary
- [x] s2.3 Revise overview.md against the literature
- [x] s2.4 Scope sign-off  (checkpoint)

## s3 - Research plan
- [x] s3.1 Hypotheses / approach families
- [x] s3.2 Experiment matrix + baselines + ablations
- [x] s3.3 Compute & time budget allocation
- [x] s3.4 User approves plan + compute cap  (input checkpoint)

## s4 - Data preparation
- [x] s4.1 Acquire / validate dataset access
- [x] s4.2 EDA: distributions, missingness, leakage risks
- [x] s4.3 Build splits (group-aware / time-based)
- [x] s4.4 Preprocessing pipeline
- [x] s4.5 Data card + sign-off  (checkpoint)  — taken autonomously; decision recorded in the stage report

## s5 - Experimentation
- [x] s5.1 Smoke tests / toy runs  — both paths clean; L40S throughput measured at 3,994 tok/s and the C sweep resized to fit
- [x] s5.2 Baselines  — harness verified; reference row B1 is complete at full counts on all four components (tool calling 0.6700 native / 0.5355 ours, structured output 0.1355, instruction following 0.8170, and a guardrail flag rate of 0.0074 with zero false alarms on the clean control arm), reads behind the 1B incumbent on tool calling, and sits at the floor on the safety axis; the 4-bit serving path is BUILT AND VERIFIED on attempt 5 (llama.cpp b10622 for sm_89, in shared storage, 196 generated tok/s over 8 slots, determinism held, a full Q4 pass priced at 1.71 h, 51% under its estimate), B2's first launch provisioned a gcp machine and ran nothing (COMPLETE, progress 0, 15 empty provider polls, no artifacts) and is relaunched as c4091ac9 pinned to aws; reading that job record also caught that the recorded queue commands for all three 4-bit rows would have run 40 items per component while labelling themselves full passes, since profile is only a label in s5-eval/main.py:327-338, and the commands are corrected; B3 (unsloth UD-Q4_K_XL) is now complete too at 0.5837 native tool calling, 0.4339 on the s4 text convention, 0.1195 structured output, 0.8022 instruction following and the same 0.0074 flag rate with zero false alarms, so the vendor's quantization-aware build beats the best cheap community quant by 5.6 points on tool calling and the two agree to within 2.4 points on the text convention, which is the ambiguity the runtime control resolves; the clean-control harness edit was therefore applied at 17:19Z once B3 was down (arm verified in storage first: 138 items, all tagged clean_corpus) rather than waiting for B4, which takes B1r, B5 and B6 off the supplementary re-score list and leaves only B1 through B4 on it; B1r (c3886996), the runtime-vs-quantization control, came down at 0.6252 native tool calling, 0.4669 on the text convention, 0.1345 structured output, 0.8115 instruction following and a 0.0074 flag rate with zero false alarms on both the frozen and the new 138-item clean arm, and it splits by axis: on the text convention it beats both 4-bit rows, so most of their shortfall there is the llama.cpp runtime rather than quantization, but on native tool calling it trails B2 (0.6252 vs 0.6392), so quantization-aware training more than recovers the runtime's cost on that axis; B4 (c319ebb1, Granite 4.0-1b, full precision) and B5 (64fa9373, Nova Function-Calling Q4_K_M) then landed together at 19:06:47Z and 19:33:21Z, both success, both verified against full artifact lists, at 4.141 and 0.642 GPU-h, giving Granite 0.7641 native tool calling / 0.1817 text convention / 0.3205 structured output / 0.7394 instruction following / 0.0000 flag rate and Nova 0.6821 / 0.2681 / 0.0710 / 0.5379 / 0.0185 with zero false alarms on both arms; the finding that closes the substage is that BOTH competitors win native tool calling and lose every other axis, and the two calling surfaces order in opposite directions (the two rows that beat us natively are the two worst on the text convention, Granite by 35.4 points), so the pre-registered tool-calling bar is a question about which surface we intend to serve rather than one number to beat and s6 should carry the two surfaces separately rather than pooled; Granite's 0.0000 flag rate is uninformative rather than clean, since a model that never flags cannot false-flag, and no baseline comes near the pre-registered 0.35 target; B6 (LFM2.5-1.2B-Base at screening) was DROPPED as redundant, since the sweep's own C1 reference arm measures the base checkpoint directly and at full budget, and both freed slots went to the s5.3 prerequisites instead; B1, B2, B3 and B4 remain on the supplementary clean-arm re-score list as zero-dependency filler work for any otherwise-idle slot; ledger cleaned in the same pass (stale total_spent 3.871 corrected to a real 10.860 of 145 across 30 entries, none left open, and an orphaned RUNNING duplicate of c4091ac9 removed without changing any recorded spend)

- [x] s5.3 Broad sweep  — ALL EIGHT ARMS TRAINED, SCORED, RE-SCORED AGAINST THE 138-ITEM CORPUS CLEAN ARM, AND COMPARED PAIRWISE. Probes pass 9ba23f0c (C1,C2p,C3,C4) and 2c91ab0d (C5a,C5b,C6,C7) both success, 8/8 arms rc=0, 0.253 GPU-h combined, 28 artifacts verified; every arm now carries 602 probe rows against 464 before. DETERMINISM: before promotion the old and new scored_probes.jsonl were joined on id, 464 shared items per arm, ZERO verdict changes on all eight arms across different machines and a rebuilt pack script -- an end-to-end reproducibility result the project had not demonstrated. Promoted --include scored_probes.jsonl --force only, deliberately excluding score.json/eval_summary.json so the full pass's summaries were not replaced by probes-only ones. GATE 1 on the corpus arm: C7 ALONE PASSES (detection 0.7222, false alarm 3/138 = 0.0217); C4 0.6519, C5b 0.6333, C6 0.6222, C1 0.6111, C3 0.6037, C5a 0.5741, C2p 0.0074, ALL failing on detection and NONE near the false-alarm ceiling -- C5a's 0.10 on the thirty-item arm was three items and reads 0.0217 on 138. RESCOPE TRIGGER DOES NOT FIRE. COMPARISON 3fec1ed8: eight arms against C1 in one call, 70 cells, 0 assertion failures, every joined rate reconciled against the arm's own eval_summary.json; 22 separate by interval, 9 survive Holm, and EIGHT OF THE NINE ARE LOSSES. The only family-corrected gain in the sweep is C7 detection +0.1111 [+0.0556,+0.1667] Holm p=0.011, against C7 BFCL native -0.0255 and tools-text -0.0287, both also family-corrected. C2p detection -0.6037 confirms the guardrail block does the detection work. C3, C5a, C6 have no surviving cell in either direction. An earlier run 08ab59c0 is SUPERSEDED AND UNCITED: it failed 16 of its own assertions (BFCL v3 ships two different live_relevance questions under the id live_relevance_3-3-0, so joining on the bare id dropped one real item from every arm and both styles) and reported 16 un-cross-checked cells (the check path named score.json, a flat card, while the per-style item_weighted rate lives in eval_summary.json) -- the disabled check is exactly the one that would have caught the duplicate. Three driver amendments, all recorded as departures in runs/s5.3-sweep.md: two detail.flagged probe cells so the reliability gate itself gets a paired test (on `correct` C7's probe delta is +0.0111 at Holm p=1.0; on detail.flagged the same 270 items give +0.1111 at p=0.011), repeats kept as <id>#k and paired by occurrence with an id-order assertion behind it, and the cross-check paths repointed. 51 driver checks, 0 failed. GATE 2 WAS READ WRONG AT 13:15Z: it is a WORST-CATEGORY floor, and C7's live_irrelevance (n=882, the largest BFCL category) is 6.80 points below C1 on its own better convention and 6.46 below on native, with five native categories past the 3.0 floor. C7 FAILS GATE 2. Also corrected: C7's IFEval is 0.7338, not the 0.7411 recorded at 13:15Z, so the gap to C1 is 1.85 against the 2.0 allowance. Gate 3 unaffected, C7 IFStruct 0.1400 >= C1 0.1375. SO NO ARM PASSES ALL THREE GATES, which neither the tie-break clause (cheapest arm passing all three) nor the rescope trigger (every arm failing gate 1) covers -- a gap in the pre-registered rule that s5.4 must close explicitly. Gate 2 is written against THE MATCHED BASE, not C1, and B1's per-category table was lost to the typed-save bug, so gate 2 has been unevaluable as written for the whole stage; the composite reading that replaced it happened to pass, which is why nobody noticed. Correcting an earlier framing: B1's scored files are NOT unretrievable, they are listed in its job record under eval_results, but no command here can fetch them since job download reaches artifacts only. Score-only replay f611bb2b QUEUED at 00:21Z (rescore_object=tidepool/s5.2/B1-fp-instruct, full profile, no weights, same graders, ~0.2 GPU-h) -> promote to tidepool/s5.3/arms/B1/ so gate 2 becomes evaluable against its own base AND B1 enters the paired comparison as an arm, putting a test behind the plan's +3.0-over-matched-base claim. Remaining before s5.4: f611bb2b lands and is promoted, the comparison re-runs with B1 in the family, then s5.4 executes the rule and records how it closed the gap. Spend 52.323 of 145. CLOSED 2026-08-27 00:55Z. B1 replay f611bb2b COMPLETE (9985 completions replayed, 0 assertion failures, generated_tokens 0, 0.074 GPU-h) and promoted to tidepool/s5.3/arms/B1/. Its logs independently confirm the duplicate-id defect: '3490 rows (3489 unique ids)', '1 prompt-hash mismatches' on both BFCL styles. RE-SCORED B1 DIFFERS FROM ITS s5.2 ROW (composite 0.6649 vs 0.6700, ifeval 0.8189 vs 0.8170) because the graders moved after s5.2; the replay is the correct comparable base and the s5.2 row must not be differenced against anything in this stage. GATE 2 AGAINST THE TRUE BASE: BFCL half passes everywhere (all eight arms +3.64 to +5.88 over B1 on best-convention composite, clearing the plan's +3.0 target; within tools_text five arms breach no category at all -- C1 -2.27, C3 +0.23, C4 -1.70, C5b +0.11, C6 -2.15 worst). IFEVAL HALF FAILS FOR ALL EIGHT: C3 and C5a -3.51, C5b -4.81, C1 -6.65, C4 -7.21, C7 -8.50, C2p -9.06, C6 -11.65, against a 2.0 allowance, and the result holds under all four IFEval conventions (the single cell anywhere under 2.0 is C3 on instruction_level_loose at -1.92; the project has recorded prompt_level_strict at every stage and switching now would be fitting the rule to the data). SECOND COMPARISON c1add251 with B1 as a ninth arm: 78 cells, 28 separate, 14 survive Holm, and C7's gain is untouched at +0.1111 [+0.0556,+0.1667] Holm p=1.18e-2 (Holm's multiplier for that rank is unchanged when the added cells are more significant). C1 vs B1 paired: BFCL native +3.15 (p=6.4e-4), tools-text +12.38 (p=3.2e-42), probe detection +11.85 on correct (p=3.4e-8) and +60.37 on flagged (p=1.3e-47), IFStruct +0.20 (p=1), IFEVAL -6.65 (p=4.8e-3). The plan's +3.0-over-matched-base claim now has a paired test behind it, and so does the regression. B1's flag delta (-0.6037, same interval) EQUALS C2p's to four decimals: the guardrail slice specifically is what creates flagging, not fine-tuning in general. Two assertion failures in c1add251, both benign -- B1 has no clean_control_object so its corpus components hash the empty sequence (da39a3ee) and can never match a non-empty one; the cells were already dropped for B1 with a note. Guard added to main.py (skip when either side selected no rows) with a regression test that fails against the old code; suite now 53 checks, 0 failed. The c1add251 record keeps its two failures rather than being re-run to look clean. Spend 52.397 of 145.
- [x] s5.4 Direction decision with researcher  (checkpoint)  — THE DECISION RULE IS PRE-REGISTERED, written 2026-08-26 08:20Z while 385e210a was at 62% and no arm had a task score; it is in the s5 report under "The s5.4 decision rule, written before the numbers" and any departure will be recorded as one. Ranking metric: BFCLv3 overall, category-macro, better of the two calling conventions per arm, exactly as B1-B6 were scored, because that is the metric the pre-registered claim is written against. EVIDENCE GATE IS THE PAIRED TEST, NOT THE RANKING METRIC: an arm beats C1 only if its item-weighted paired delta survives Holm correction across the whole s5-compare family, and where the macro composite and the paired delta DISAGREE IN SIGN both are reported and no winner is claimed on that axis. Three gates first: (1) detection >= 0.70 with false-flag <= 0.15 on the clean items -- AMENDED 08:34Z on a configuration fact, before any arm had a score: clean_control_object is empty in pack.yaml and no arm queue script sets it, so NO ARM IS SCORED AGAINST THE 138-ITEM CORPUS CLEAN ARM. Not fixed by setting it for later passes, because C1 would then have no corpus cell for any arm to pair against; all eight arms stay identical and gate 1 is evaluated on the 30-item synthetic arm, which bounds the false-flag RATE only to about one item in thirty but does support the paired comparison s5.4 needs (an arm flagging a fifth of clean returns puts six discordant pairs against C1's zero). A probes-only pass with clean_control_object set is queued for every arm after the four scoring passes and before s5.5, 138 extra items an arm and a few card-minutes for all eight, and THE RESCOPE TRIGGER IS READ AGAINST THAT, not against the thirty; (2) no BFCLv3 category more than 3.0 below the matched base and IFEval no more than 2.0 below; (3) IFStruct at or above C1's. IF NOTHING SEPARATES (the likely outcome, since three of four arms tied within 0.0006 on val_loss) s5.4 does NOT pick the numerically highest arm: it picks the cheapest recipe passing all three gates, ranked r16 > r64 > full-parameter, no replay > 1% > 5%, reference mixture > raw, and records that the choice was made on cost because the arms were indistinguishable. A recorded non-separation is a finding about the sweep and goes in the paper as one. RESCOPE TRIGGER: if EVERY arm fails gate 1, the guardrail axis is not reachable from this training set, s5.5 has no direction worth detailing, and that stops for the operator rather than being decided under autonomous mode. ||| EXECUTED 2026-08-27 00:55Z UNDER AUTONOMOUS MODE, NOBODY REVIEWED IT. See the s5 report section 'The s5.4 decision: H2 is falsified as written, and s5.5 goes at the one thing that broke'. (a) THE TWO GATE-1 BARS CONFLICT: the plan's stop/go into D asks >= 0.35 (set at s5.2 on 2026-08-25 with an explicit reason -- 0.70 is the s6 criterion and demanding it of a screening arm would kill the preference rung), while this s5.4 rule asks >= 0.70, restating the s6 criterion without addressing the amendment written the day before. Both applied and both reported: at 0.35 seven arms pass (only the C2p ablation fails), at 0.70 only C7. NEITHER DECIDES ANYTHING because gate 2 removes every arm. (b) NO ARM PASSES ALL THREE GATES, on the IFEval half of gate 2, by 1.5 to 9.7 points. (c) H2 IS FALSIFIED IN ITS OWN WORDS ('no arm holds all five guardrails within 2.0 points while gaining >= 3.0 on BFCLv3'): both halves are met, and the plan itself says a forced trade-off is a reported finding, not a failure. H3 takes a hit the other way and it is a positive: H3 predicted SFT alone does not reach 0.70 flag rate, and C7 reaches 0.7222 by SFT alone, so the claim that the preference rung is REQUIRED is contradicted by one arm. (d) THE RULE HAD NO BRANCH FOR THIS: the tie-break presupposes a non-empty passing set, the rescope trigger needs EVERY arm to fail gate 1 and C7 passes at either bar. EXTENSION RECORDED AS AN EXTENSION, NOT AN APPLICATION: with an empty passing set and no rescope trigger, s5.4 picks no winner and does not stop; it records the falsification, names the single binding constraint and points s5.5 at it, choosing the direction by what is closest to passing CONDITIONAL ON THE CONSTRAINT BEING REMOVED rather than by today's ranking (which would hand the stage to C3 or C5a, both mid-pack on detection and both failing gate 1 at 0.70). (e) DIAGNOSIS, CORRECTED AT 01:20Z AFTER READING THE POOL RATHER THAN ASSUMING IT. My first reading -- that replay drew on the project's own agentic prompt pool and so carried no instruction-following behaviour -- IS WRONG. The pool is antidoom-mix-v1.0, 447,053 general-quality prompts (pubmedqa 11.2%, ultrachat 11.2%, mmlu_auxiliary 11.2%, lmsys_arena 10.8%, metamathqa 10.7%, evol_codealpaca 10.5%, ultrainteract 10.4%, math_qa 6.7%), decontaminated against IFEval by the s4 13-gram index. IT CONTAINS open_perfectblend_autoif AT 46,734 ROWS = 10.5%, which is verifiable-constraint instruction following of exactly IFEval's kind ('Do not use the word important anywhere in your response', 'Your response should contain at least 200 characters', 'Your response must be presented in a report format'), plus ifstruct_train_generated at 0.3%. THE FROZEN BUFFER IS A PROPORTIONAL HASH-RANK SAMPLE OF 8,000 PROMPTS, so ~840 are constraint-bearing, and at replay_frac 0.01/0.05 that slice is ~0.1%/~0.5% OF TRAINING TOKENS. So the axis was never tested, it was sampled once at a dose two orders of magnitude below anything that would hold a capability in place. That is why 1% gives 0.7837, 5% gives 0.7708, and raising the fraction fivefold made it slightly WORSE. (f) DIRECTION FOR s5.5, unchanged in substance and sharper in mechanism: SEPARATE THE TWO LEVERS THE SWEEP CONFOUNDED, at the same fixed 64M tokens every C arm got, with C7 as the origin (C7 is already raw mix at r16 with no replay). R1 replay_frac 0.05 with the buffer REWEIGHTED to ~half constraint-bearing (isolates composition); R2 replay_frac 0.20 with the PROPORTIONAL buffer (isolates dose); R3 replay_frac 0.20 reweighted (both). C7's mixture because role balancing is the one thing the sweep resolved (its +0.1111 is the only family-corrected gain among 78 cells); rank 16 because rank 64 bought nothing and cost the most IFEval of any arm (-11.65), which is also the tie-break's own cost ordering. NEEDS A FRESH SELF-DISTILL PASS for the reweighted buffer, since reweighting changes which prompts are sampled and the completions must be B1's own over those prompts. KNOWN CONFOUND, stated up front: raising replay at fixed budget displaces guardrail and tool data, so R2/R3 should give some detection back; how much is what s5.5 measures, and R1 exists because it buys protection at a fifth of the displacement. PASS CONDITION: IFEval within 2.0 of B1 while holding detection >= 0.7222 and false-flag <= 0.15. If that fails, the forced trade-off is the paper's result and the delivered checkpoint is chosen on the operator's stated priority order, tool calling first. Spend 52.397 of 145, so 92.6 GPU-h remain; the ladder is sized inside that and its cost is stated before it is spent, per the operator's standing note.
- [ ] s5.5 Detailed runs  — IN FLIGHT as job d4a7d46b (task s5-sft-pack cfc6dec7, one L40S on NEBIUS, run_tag s5.5-packR, queued 2026-08-27 04:25Z). Five arms on one card: RBP and RBC generate two 32,000-prompt self-distillation buffers side by side (RBP strata {} proportional; RBC strata {"open_perfectblend_autoif":0.47,"ifstruct_train_generated":0.03}, exactly half constraint-bearing), then R1 (replay_frac 0.05 of RBC), R2 (0.20 of RBP) and R3 (0.20 of RBC) train on C7's recipe at the same fixed 64M-token budget every C arm got. CONTRASTS: R3-R2 isolates COMPOSITION at a fixed dose, R3-R1 isolates DOSE over one and the same buffer, R1-C7 asks whether a small reweighted dose moves IFEval at all. PASS CONDITION unchanged from s5.4: IFEval prompt_level_strict within 2.0 of B1 while holding detection >= C7's 0.7222 and false-flag <= 0.15; failing that, the forced trade-off is the paper's result. SHARES ARE 0.47/0.03 AND NOT THE 0.45/0.05 s5.4 WROTE: the decontaminated pool holds 46,734 open_perfectblend_autoif rows but only 1,279 ifstruct_train_generated (IFStruct is one of this project's eval sets, so the s4 13-gram index dropped 18,721 of its 20,000 training rows), and at n_prompts 32,000 a 0.05 share asks for 1,600 the pool cannot supply, which replay.py fails the run over. Buffers are 32,000 rather than s5.3's 8,000 so a 20% dose (12.8M of 64M tokens) makes at most one pass; c5b_dose_tokens raised to 12,800,000 so the generator's repeat-count gate checks the dose these arms actually take. TWO GENERATORS ON ONE CARD NEEDED A FIX: pack_provides resolves a produced object by CONFIG KEY, so both naming replay_object would have had the second registration overwrite the first and two of three trainers would have silently read the wrong buffer; pack.yaml now declares replay_object_proportional and replay_object_constraint separately, with a scheduling test over this exact five-arm topology. Peak concurrent demand 37.0 GB of a 43.9 GB limit (RBP still generating while RBC has released R1 and R3). COST STATED BEFORE SPENDING per the operator note: 11-12 GPU-h estimated; a dose ladder at 0.10/0.35 plus a seed replicate would be ~8 more and is deliberately NOT queued. FIRST SUBMISSION 31403d7d WAS REFUSED AT LAUNCH: AWS returned PendingVerification for ap-northeast-1a, no arm started, 0.033 GPU-h charged, re-queued unchanged on Nebius per the provider note's L40S try order. Spend 52.430 of 145. ON THE NEXT WAKE: read runs/s5.5-replay-ladder.md, verify lab job artifacts d4a7d46b -e tidepool is non-empty before recording anything, then score the three arms through s5-eval and compare them through s5-compare against C7 and B1.
- [ ] s5.6 Final tuning + ensembling

## s6 - Evaluation & analysis
- [ ] s6.1 Held-out eval vs. success criteria
- [ ] s6.2 Subgroup / fairness analysis
- [ ] s6.3 Calibration analysis
- [ ] s6.4 Error analysis
- [ ] s6.5 Robustness / ablations
- [ ] s6.6 Story checkpoint: agree the paper's story  (input checkpoint)

## s7 - Reporting & delivery
- [ ] s7.1 Academic paper (LaTeX → rendered PDF)
- [ ] s7.2 Model card
- [ ] s7.3 Reproducibility packages (core kit + chosen artifacts)  (input checkpoint)
- [ ] s7.4 Final sign-off  (checkpoint)
