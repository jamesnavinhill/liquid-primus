# Tasks: tidepool — improving the LFM2.5 on-device family for our agent stack

Status: `[ ]` todo · `[~]` in progress · `[!]` blocked · `[x]` done · `[-]` cancelled (the operator closed the project)
Active stage: s5

> Project notes (from the operator's plan in `documents/liquid-primus/`): the goal is
> improved LFM2.5 checkpoints plus a hybrid MoE family, specialized to this
> organization's stack, across four on-disk device tiers, with 4-bit GGUF forms that hold
> the full-precision quality bar. Priority when goals conflict: tool-calling and agentic
> reliability first, then on-device efficiency, then no regression in general quality.
> Reference device is a single 8 GB consumer GPU. The operator wants artifacts mirrored
> to their GitHub and Hugging Face repos as they land.

## s0 - Intake & scoping
- [x] s0.1 Capture initial prompt → initial-prompt.md
- [x] s0.2 Self-scope → provisional overview.md + assumptions

## s1 - Literature review
- [x] s1.1 Formulate research queries
- [x] s1.2 Run literature searches (academic-research CLI); copy papers → stages/s1-literature-review/papers/

## s2 - Research summary
- [x] s2.1 Extract relevant learnings per paper (link back to papers/<id>.md)
- [x] s2.2 Synthesize focused research summary
- [x] s2.3 Revise overview.md against the literature
- [x] s2.4 Scope sign-off  (checkpoint)

## s3 - Research plan
- [x] s3.1 Hypotheses / approach families
- [x] s3.2 Experiment matrix + baselines + ablations
- [x] s3.3 Compute & time budget allocation
- [x] s3.4 User approves plan + compute cap  (input checkpoint)

## s4 - Data preparation
- [x] s4.1 Acquire / validate dataset access
- [x] s4.2 EDA: distributions, missingness, leakage risks
- [x] s4.3 Build splits (group-aware / time-based)
- [x] s4.4 Preprocessing pipeline
- [x] s4.5 Data card + sign-off  (checkpoint)  — taken autonomously; decision recorded in the stage report

## s5 - Experimentation
- [x] s5.1 Smoke tests / toy runs  — both paths clean; L40S throughput measured at 3,994 tok/s and the C sweep resized to fit
- [x] s5.2 Baselines  — harness verified; reference row B1 is complete at full counts on all four components (tool calling 0.6700 native / 0.5355 ours, structured output 0.1355, instruction following 0.8170, and a guardrail flag rate of 0.0074 with zero false alarms on the clean control arm), reads behind the 1B incumbent on tool calling, and sits at the floor on the safety axis; the 4-bit serving path is BUILT AND VERIFIED on attempt 5 (llama.cpp b10622 for sm_89, in shared storage, 196 generated tok/s over 8 slots, determinism held, a full Q4 pass priced at 1.71 h, 51% under its estimate), B2's first launch provisioned a gcp machine and ran nothing (COMPLETE, progress 0, 15 empty provider polls, no artifacts) and is relaunched as c4091ac9 pinned to aws; reading that job record also caught that the recorded queue commands for all three 4-bit rows would have run 40 items per component while labelling themselves full passes, since profile is only a label in s5-eval/main.py:327-338, and the commands are corrected; B3 (unsloth UD-Q4_K_XL) is now complete too at 0.5837 native tool calling, 0.4339 on the s4 text convention, 0.1195 structured output, 0.8022 instruction following and the same 0.0074 flag rate with zero false alarms, so the vendor's quantization-aware build beats the best cheap community quant by 5.6 points on tool calling and the two agree to within 2.4 points on the text convention, which is the ambiguity the runtime control resolves; the clean-control harness edit was therefore applied at 17:19Z once B3 was down (arm verified in storage first: 138 items, all tagged clean_corpus) rather than waiting for B4, which takes B1r, B5 and B6 off the supplementary re-score list and leaves only B1 through B4 on it; B1r (c3886996), the runtime-vs-quantization control, came down at 0.6252 native tool calling, 0.4669 on the text convention, 0.1345 structured output, 0.8115 instruction following and a 0.0074 flag rate with zero false alarms on both the frozen and the new 138-item clean arm, and it splits by axis: on the text convention it beats both 4-bit rows, so most of their shortfall there is the llama.cpp runtime rather than quantization, but on native tool calling it trails B2 (0.6252 vs 0.6392), so quantization-aware training more than recovers the runtime's cost on that axis; B4 (c319ebb1, Granite 4.0-1b, full precision) and B5 (64fa9373, Nova Function-Calling Q4_K_M) then landed together at 19:06:47Z and 19:33:21Z, both success, both verified against full artifact lists, at 4.141 and 0.642 GPU-h, giving Granite 0.7641 native tool calling / 0.1817 text convention / 0.3205 structured output / 0.7394 instruction following / 0.0000 flag rate and Nova 0.6821 / 0.2681 / 0.0710 / 0.5379 / 0.0185 with zero false alarms on both arms; the finding that closes the substage is that BOTH competitors win native tool calling and lose every other axis, and the two calling surfaces order in opposite directions (the two rows that beat us natively are the two worst on the text convention, Granite by 35.4 points), so the pre-registered tool-calling bar is a question about which surface we intend to serve rather than one number to beat and s6 should carry the two surfaces separately rather than pooled; Granite's 0.0000 flag rate is uninformative rather than clean, since a model that never flags cannot false-flag, and no baseline comes near the pre-registered 0.35 target; B6 (LFM2.5-1.2B-Base at screening) was DROPPED as redundant, since the sweep's own C1 reference arm measures the base checkpoint directly and at full budget, and both freed slots went to the s5.3 prerequisites instead; B1, B2, B3 and B4 remain on the supplementary clean-arm re-score list as zero-dependency filler work for any otherwise-idle slot; ledger cleaned in the same pass (stale total_spent 3.871 corrected to a real 10.860 of 145 across 30 entries, none left open, and an orphaned RUNNING duplicate of c4091ac9 removed without changing any recorded spend)

- [~] s5.3 Broad sweep  — PASS 1 (C1, C3) IS DOWN AT FULL ITEM COUNTS (2.473 GPU-h, both arms rc=0, scored files promoted to storage); PASS 2 (8b7bad6f, C2p/C7) has cleared BFCL at both calling conventions for both arms and is now in the ifeval/ifstruct phase (C2p and C7 both climbing through their 2,000-item sets, no stall); PACK B (1df3bf2b, the remaining four training arms C4/C6/C5a/C5b) IS HEALTHY AT 45% and climbing steadily, no OOM or stall. Check-in at 2026-08-26T11:47:54Z found both jobs progressing normally; no action needed. CORRECTION on re-inspecting the finished console logs rather than the live progress line: the earlier "ZERO out-of-memory splits" claim below was wrong — each of C1 and C3 hit exactly one OOM split near the tail of bfcl/tools_text, both retried successfully as 8+8 with no data lost and no assertion failure, so the 11 GB ceiling holds with one graceful degrade per arm rather than none; full detail and scores in runs/s5.3-sweep.md at 10:30Z. THE 11 GB CEILING HOLDS, FULL-COUNT SCORING COSTS ABOUT 8 CARD-HOURS FOR ALL EIGHT ARMS, AND THE COMPARISON THAT DECIDES s5.4 IS BUILT, TESTED AND REGISTERED. 385e210a (C1 + C3, all four components, full item counts, two arms on one L4 under 11 GB ceilings) has cleared BFCL's full 3,490 items at the first of two calling conventions plus a third of the second, per arm, with ZERO out-of-memory splits and gen.py's halving path never fired. A COST CLAIM MADE HERE AT 08:12Z WAS WRONG AND IS WITHDRAWN: the supervisor's progress line reports each child's last generated i/n and the harness counts per component, so 3,490 is BFCL alone at one convention rather than the run, and the '130 items a minute' behind the one-card-hour figure was the short-prompt front of one component, since generate() runs its prompts shortest first. The anchor that should have been used is 4350ce4e, B1's full pass over the identical workload solo on an L4 in 85 minutes and 1.42 card-hours; the pair is estimated at 2.0 hours and four passes at about 8 card-hours of 116 unspent. THE SCREENING FALLBACK STAYS WITHDRAWN, on the corrected price rather than the wrong one: screening would save about 6 card-hours and cost the paired test most of its power, and power is the whole point when three of four arms sit 0.0006 apart on validation loss. Full details of the misreading and its correction are in runs/s5.3-sweep.md at 09:10Z. queue_arms_pass2.sh (C2p + C7, identical settings) is written and waiting on a slot; the account cap is two and 1df3bf2b holds the other. THE DECISION MACHINERY: jobs/s5-compare/ is registered as task ecd12a36-da98-4a18-bc38-2889905746e9, CPU-only, no GPU-hours. It joins arms item by item against C1, which takes item difficulty out of the comparison and is the only reason a two-point gap over a few thousand shared items is resolvable; puts a 10,000-resample percentile-bootstrap interval and an exact two-sided McNemar test on every arm-by-component cell; and applies Holm-Bonferroni across the whole family, because 28 cells against one reference produce a nominally significant result by chance about once even when every arm is identical. queue_compare.sh takes every arm in ONE call for that reason: splitting them over two jobs would correct each half against a family half the size and report p-values too small in both. It is a job and not a local script for provenance, since a bootstrap interval is a reported statistic. TWO INPUT FACTS CORRECTED WHILE BUILDING IT: the per-item correct verdict lives in scored_*.jsonl, and the earlier plan to read completions_*.jsonl would have produced an EMPTY comparison because those files hold raw generations and no verdict; and scored_probes.jsonl is split into its four populations (detection, synthetic clean, corpus clean, stack idiom) rather than averaged, because an arm can raise its detection rate purely by flagging more often and pooling hides that trade. 67 checks pass (31 statistics, 36 driver over fixtures with the SDK stubbed); the load-bearing fixture has an item-weighted delta of +0.1 and a macro delta of -0.125 over the same ten items, so a run reporting one column would report the OPPOSITE DIRECTION, and both are reported with the macro figure marked as carrying no test. promote_scores.py moves an arm's scored files out of a job's artifact list into tidepool/s5.3/arms/<arm>/, stripping the <arm>__ prefix, computing nothing, and refusing to overwrite an existing object unless forced; dry-run verified against 37b50115. Everything below this line is history from before the full-count measurement: THE SCORING HARNESS IS PROVEN AGAINST REAL CHECKPOINTS AND SIZED ON A MEASUREMENT. The four-arm smoke 97939c69 (C1/C2p/C3/C7, 16 items a component, L4:1 aws, 0.061 GPU-h) exited 0/4 arms completed and answered all three of the questions that mattered. (1) C3's full-parameter checkpoint is recognised and loaded as a whole model: its archive carries config.json and a 2.34 GB model.safetensors and no adapter_config.json, adapters.kind() classifies it FULL, and the hf branch loads it as the model with nothing merged onto it -- the first time that path has run against a real checkpoint. (2) All four Pack A adapters resolve out of tidepool/s5.3/arms/<arm>/adapter.zip after the hand repair, with the native chat template accepting tool turns exactly as it did for B1-B6. (3) THE CEILING KILLED EVERY ARM, NOT THE CARD: all four died in prefill on their first BFCL batch with 4.41 GB allocated, 772 MB wanted and 4.78 GB allowed, and C7 was killed with 17.33 GiB of the card still free, because set_per_process_memory_fraction enforces the cap whatever the card has spare. The 772 MB is one FFN activation, about 49,400 bf16 tokens over an 8192-wide intermediate, i.e. batch 16 at roughly 3.1k tokens a prompt, which is what BFCL's live categories carry in tool schemas. NONE OF THE TRAINING FIGURES APPLY TO SCORING: 7.5 GB for a LoRA r16 arm, 11.4 for entropy-weighted and about 22 for full-parameter are gradients and optimizer moments, and at inference a merged r16 adapter and a full-parameter checkpoint are both a 1.2B bf16 model at 2.34 GB of weights, so what an eval arm holds is prefill plus KV cache, a little over 5.2 GB at batch 16 plus about 0.3 GB of CUDA context. TWO ARMS AN L4 AT 11 GB EACH is therefore the sized figure and four does not fit, recorded in pack.yaml beside pack_gb so the next reader does not repeat the guess; packing also buys much less here than in training, since generation saturates the card where training leaves it idle between steps, and two arms over four jobs costs about the same card-hours as four arms over two. An undersized ceiling fails on the first batch of the first component, so the whole thing was over in under four minutes, which is what made sizing the real pass on evidence cheap. THE SERVING LOOP NOW DEGRADES INSTEAD OF DYING: gen.py halves a group that will not fit and retries down to a single prompt, and only a single prompt that cannot run at all is still fatal; wasted attempt time is banked in oom_seconds and kept out of the tok/s denominator, and oom_splits and smallest_batch reach score.json so a ceiling set too low is visible in the result. The split is safe on the premise gen.py's docstring already carried (greedy argmax with left padding does not depend on prompt grouping) and it breaks the older claim that batch composition is a function of item order alone, which the docstring now corrects. test_gen_oom_split.py covers it with torch stubbed and a fake model refusing any batch above a set size: 16 checks over an untripped ceiling, a ceiling admitting 4 (halving 16,8,4,4,8,4,4 with order preserved and the cache dropped once per failed attempt), an odd group of 5, a single prompt that cannot run, and the counters reaching throughput(). ONE RECORDED PLAN WAS DEPARTED FROM DELIBERATELY: s5.2 chose a screening profile for ranking the eight arms at 0.55 GPU-h each, which holds when arms are separated by a visible margin, and three of the four trained arms sit within 0.0006 on val_loss, so sampling noise is now the thing most likely to decide the ranking. 385e210a therefore scores C1 and C3 at FULL item counts (two arms, L4:1 aws, 11 GB ceilings, run_tag s5.3-arms) with card, batch size, item counts and component list all the B1-B6 settings unchanged, since arms separated by 0.0006 cannot also be separated by a change of hardware; pack.yaml's header already fixes the L4 as non-negotiable for that reason. Full counts price at about 2.5 GPU-h an arm solo, near 3.5 card-hours a pair and about 14 for all eight against 116 unspent; the first pair runs at full counts to MEASURE that figure, and if it lands materially above the estimate the remaining three pairs get screened and the report says which rows were taken which way. C2p and C7 wait on a GPU slot (cap is 2, both held: 385e210a and Pack B). Pack B (1df3bf2b) is training at 14% and its four adapters will need the same hand move into tidepool/s5.3/arms/ that Pack A's did, because the patched supervisor is deliberately NOT on the sweep task while Pack B is live on it. Spend 28.71 of 145. Everything below this line is history from before the scoring harness was sized: HALF THE SWEEP IS TRAINED AND val_loss DOES NOT SEPARATE THE ARMS. Pack A (7e8ca5f9) finished at 07:01:17Z after 9h16m with 3/3 arms at rc=0 on the full 64.0M-token budget, zero assertion failures and all 18 per-arm artifacts plus pack_summary.json verified before mirroring: C2' 0.1511, C3 0.1513, C7 0.1565 against the reference C1 at 0.1517, peak 25.51 of a 47.7 GB card, charged 9.198 GPU-h for 28.544 of 145 (the ledger's stale total_spent of 16.345 was corrected to the real 19.346 across 37 entries in the same pass). Three of the four arms sit inside 0.0006 of each other, which at one seed apiece is a tie rather than a ranking: full-parameter tuning of all 1.2B weights lands within noise of a rank-16 adapter, and removing the guardrail block moves val_loss 0.0006 in the BETTER direction, which is expected by construction since C2' returns those tokens to the base mix and what the block buys shows up in the probe flag rate rather than in general-mix cross-entropy. Only C7 separates, so role balancing against uniform sampling is worth about 0.005 and is the one clean effect in the table. THE CONSEQUENCE LANDS ON s5.4: the sweep's own selection metric cannot pick a direction, so the decision has to rest on the s5.2 task metrics scored over each arm's adapter, which promotes the evaluation harness from an s6 concern to the critical path. Both freed cards were filled at 07:21Z: Pack B (1df3bf2b, L40S:1 aws, run_tag s5.3-packB) is the last four arms plus the replay buffer two of them read, with RB generating via replay.py, C4 and C6 training from the start and C5a/C5b held on pack_after until RB exits clean, which is the first run of the scheduling path in anger; and the packed evaluation smoke (arms E1 bfcl+probes and E2 ifstruct+ifeval at 16 items each on the published checkpoint, L4:1 aws) earns the harness a job id before s5.4 leans on it -- its first attempt (18c83b6b) died 3m46s in because the supervisor rejected its own valid input: a `-p key={json}` argument is typed on the way in, so job_data stores pack_overrides as a dict, str() on a dict is a Python repr, and json.loads will not take single quotes. pack.py now accepts a mapping as-is with the repr as a fallback, both shapes are in the scheduling contract test, and the re-queue is 37b50115; 0.063 GPU-h was charged for the launch. Worth naming because the failure mode is invisible in a smoke that passes locally: every local test built the value with json.dumps, which is the one path the platform does not take. Both stayed on AWS: the steering note's L40S row is AWS (g6e), Nebius, RunPod in try order, Pack A had just succeeded there, and keeping Pack B on AWS leaves 7 of 8 arms on one vendor with C1 the lone Nebius exception, which is a better comparability position than a 4/4 split. Allowance re-read before launching: 2 GPUs at a time, 0 in use, 194h55m left, nothing binding. ONE EARLIER FRAMING WAS WRONG AND IS CORRECTED: the nine-minute sizing trial called packing a 1.51x throughput win by comparing against 2,833 tok/s from the C4 entropy-weighted smoke over 30 steps, the most expensive arm timed across its warmup; against C1's real solo rate of 5,229 tok/s on the same provider, Pack A's 6,302 tok/s aggregate is 1.21x, and in card-hours 3.07 per arm against 3.48 solo, a 12% saving rather than 34% -- the win that justifies packing is the parallelism the operator named, since three arms on one card left the second card free and four arms finished in a single 9-hour window under a cap that would otherwise have run them in two rounds. C3 was also placed first as the presumed slowest arm and was not the bottleneck (8.07h against C7's 9.17h, because the raw-mix arm does 15% more steps), so step count predicts the tail rather than tuning method. Everything below this line is history from before Pack A completed: C1 IS COMPLETE (attempt 3, nebius): 8416/8416 steps, val_loss 0.1517, 0 assertion failures, 3.001 GPU-h, all 6 artifacts uploaded; mirrored into budget.json (spend now 19.346 of 145) and runs/s5.3-sweep.md. PACK A (7e8ca5f9) is at 93% with only C7 left (~9196/9697 steps, ~95%, climbing steadily, no OOM/stall) — C3 and C2p have both already exited rc=0. One job outstanding; next check-in should see Pack A finish, at which point its full spend gets recorded and queue_pack_b.sh starts Pack B on the freed slot. Everything below this line is history from before C1 completed: C1'S RE-RUN WAS REFUSED A MACHINE AND IS NOW ON NEBIUS; PACK A AT 60%. Attempt 2 (5d9be511, single-arm pack on aws, queued 00:55Z) never started: AWS answered RunInstances in eu-central-1a with PendingVerification, an account-region validation on the platform's aws account, at progress 0 with no resources block and no artifacts, charged a MEASURED 0.000 GPU-h. The provider note (updated 2026-08-23T22:48:37Z) settled the response rather than my judgement: AWS GPU quota exists only in us-east-1/us-east-2 and every other region is zero, and the L40S source row is AWS (g6e), Nebius, RunPod in try order, worked left to right, so the launch had landed in a region with no quota at all and `lab task queue` exposes --provider but no --region. C1 re-queued as 1728ed4a on NEBIUS at 03:46Z, every parameter identical (task cfc6dec7, -p arms=C1 -p pack_gb=16 -p stall_minutes=45 -p run_tag=s5.3-C1), reaching launching_cluster inside a minute. COMPARABILITY CAVEAT, recorded in runs/s5.3-sweep.md rather than assumed away: C1 is the reference every arm is measured against and is now the only arm renting its L40S from a different vendor; card model, recipe, seed and the 64.0M-token budget are unchanged, so the exposure is vendor-level, and a ~4.45 GPU-h re-run on aws is the check if its curve sits oddly against the packed arms. Three C1 attempts now, each with a DISTINCT failure signature and a distinct fix (silent stall, then a refused launch, then this one), so each is progress rather than a loop, and nothing about what the arm measures has been weakened to get a job through. If a later Nebius launch is refused with ResourceExhausted compute.instance.count, that is the account-wide Nebius cap shared across tenants, costs nothing, and the next source is RunPod. The 23:51Z blocker was retired this run: the operator had already answered it at 00:46Z and the marker was left live, which would have parked the project on a settled question while a slot sat idle. Spend unchanged at 16.345 of 145. Everything below this line is the pre-blocker history: C1 attempt 1 (e4bd367a) ran clean to step 2940/8416 Attempt 1 (e4bd367a) ran clean to step 2940/8416 (22.9M of 64.0M tokens, loss 0.3024, 5229 tok/s) then produced no further step line for 2h47m while still reporting RUNNING at 31% on a reachable machine, with no OOM, traceback, scheduled eval or preemption signal anywhere in its logs; stopped at the operator's direction 2026-08-26 00:46Z and charged in full at 4.059 GPU-h, spend now 16.345 of 145. The cause of the stall is still unidentified and may never be; what made it expensive is mine, and both halves are fixed and REGISTERED (both tasks re-registered 00:55Z: cfc6dec7 s5-sft-pack and 37186b22 s5-sft-sweep). FIX 1, nothing was recoverable because the trainer wrote weights ONLY after its final step: main.py now checkpoints on optimizer-step boundaries to tidepool/s5.3/ckpt/<run_tag>/<arm>/, carrying weights, optimizer and scheduler state, and resumes from the furthest of two alternating slots; a 21-value recipe fingerprint refuses a resume across a changed recipe, the pointer is written only after its slot upload lands, a partial weight load is refused rather than half-applied, the resume re-enters mid-epoch by row offset (valid because the loader is shuffle=False, drop_last=True), throughput is measured against the tokens the resuming job itself saw, and pack.py leaves ckpt-* out of the job's artifacts. Two defects were caught in review before any of it was registered, both silent: the checkpoint prefix was keyed on run_tag alone, and a pack hands every child ONE config, so four arms on a card would have written four recipes over one pair of objects (prefix now carries the arm); and the cadence was a flat twenty per run, which is a few hundred MB for a LoRA arm but ~14 GB for the full-parameter arm, i.e. a quarter of a terabyte of upload for a four-hour job (cadence is now derived from the payload, as many as fit under ckpt_max_gb=60 of total upload, clamped 4..20 and never more than one per 50 steps). test_ckpt.py covers all of it in 18 checks and every one was mutation-tested: an earlier draft of the suite passed while a dropped arm in the fingerprint, a disabled mismatch guard, an unconditional pointer write and a stubbed partial-load check all went undetected, so the checks were rewritten over the syntax tree rather than over the source text. FIX 2, the stall watchdog: pack.py has had stall_minutes since the packing rewrite and the solo path never did, which is why nothing noticed for 2h47m. Rather than duplicate the watchdog, C1 was RELAUNCHED AS A SINGLE-ARM PACK at 00:58Z (job 5d9be511, task cfc6dec7, -p arms=C1 -p pack_gb=16 -p stall_minutes=45 -p run_tag=s5.3-C1): a packed arm runs the same main.py, so the reference recipe stays comparable with the packed arms it is the reference for, and packed C4 consuming exactly the solo C4 smoke's 238,195 tokens is the standing proof of that. Pack A (7e8ca5f9) was unaffected throughout (a task edit does not touch a launched job) and is at 30%. Everything below this line is the pre-blocker history: the data component's first run (a42a6c21) failed six of its nine gates and wrote nothing, which is the gates working: its decontamination rule dropped 2,166 of 2,166 rows on a 13-gram that is the probe system preamble written at s4 to match the corpus's own rendering convention, and its no-fabrication check tested single-digit payload leaves as substrings of prose; both are fixed and measured against the held-out split (0 of 156 rows flagged on question overlap, 13 by two tighter nets), mode assignment now falls through the rotation for the 494 sources whose payloads echo no call argument, a 23-check fixture over the real probe bank covers every fix, attempt 2 (f11afe02) then wrote 5,202 rows and 2.87M tokens and cleared seven of nine gates including detector coverage at 0.704, failing only on a no-fabrication check that discarded 34.9% of sources because corpus tool returns echo the called function's name back in the payload while the target names the tool (forbidden list now scoped to the whole prompt: 19 of 73 held-out leaks down to 0) and on a clean-control floor of 200 the test split cannot reach at 68 items (arm now drawn from both held-out splits, floor 100 with the false-flag-ceiling reason stated); attempt 3 (7248507b) passed all nine gates in 4 m 53 s on CPU and wrote 7,988 rows and 4.35M tokens to tidepool/s5.3/tooldata/ with zero target leaks, zero question-gram drops, detector coverage 0.6838 inside the [0.60, 0.90] band and a 138-item clean control arm drawn 70/68 from the validation and test splits, so the guardrail supervision is built and the data half of s5.3 is done; the corpus ceiling is about 4,000 pairs rather than the 24,000 the parameter allows, since only 4,031 of 494,341 training rows carry a usable tool return; the stop/go gate into D has been amended in plan.md to test the pre-registered criterion (flag rate >= 0.35, false flags <= 0.15) instead of a regression a 0.0074 rate makes impossible; one imbalance recorded for the sweep: silently_truncated carries 1,042 rows against roughly 580 per common mode because it absorbs the 494 wrong_entity skips through the fixed rotation order, and a per-source random offset is the one-line fix if the model over-fits that phrasing; the sweep task is now authored and registered as s5-sft-sweep (37186b22): one code path for all eight arms selected by -p arm=<id>, the tool_guardrail role wired into the sampler as a second source in the same id space, the 64.0M-token per-arm budget carried as an explicit parameter that the run asserts against itself, the guardrail block dosed at 2 passes (8.70M tokens, 13.6% of budget) and taken out of the budget rather than added to it so C2' is cost-matched, entropy-weighted loss for C4 centred on the batch mean so beta cannot act as a hidden learning-rate multiplier, a full-parameter mode for C3 at LR 1e-5 with micro-batch 1 and accumulation 16 to hold the effective batch at 16, replay wired for C5 and failing loudly when no replay set is configured, and all arms scored on plain cross-entropy rather than their own objective; a CPU fixture test (test_sampler.py, 25 checks, all passing) covers equal cost per arm, guardrail presence and absence, and identical base-row order across arms; the design record is in runs/s5.3-sweep.md; no sweeps: block was used and the reasoning is recorded there; the 138-item clean control arm is now WIRED into s5-eval through a new clean_control_object setting, reported as its own false-flag rate beside the frozen 30-item arm and pooled in a third key, with a nine-check test (test_probes_additive.py) proving none of the eight pre-existing summary keys move and the new keys read empty when the arm is unconfigured, which caught a real additivity break where the envelope-depth table aggregated over every tool_return row and would have changed what depth meant for the four published baseline rows (the corpus arm now has its own depth_clean_corpus bucket); the edit is AUTHORED AND TESTED BUT NOT APPLIED to the server, since two baseline rows are running against the current harness, and it goes up once s5.2's rows are down; the replay generator is now sized and fixed: it had emitted neither the n_tok field the sweep's sampler sizes each role by nor a gzip suffix, so the buffer would have indexed as zero available tokens and C5a and C5b would have trained the plain reference recipe while reporting a replay fraction, with every guardrail passing, and it now counts tokens with the training tokenizer and template, writes replay.jsonl.gz, places it at tidepool/s5.3/replay/ since the arms load replay from storage rather than from a job artifact, and fails if the placement fails; the run at size is 8,000 prompts on L40S:1 pinned to aws, a strict superset of the s5.1 smoke's 64 under the same salt, priced at well under an hour and asserting that C5b's 3.2M-token dose cannot replay the buffer more than twice over; every remaining baseline row's weights, quant file and tokenizer were confirmed to resolve before queueing; still to do before the arms run: apply the s5-eval edit, give B1 a supplementary pass over the new arm, smoke the sweep task on arm C4, and run the replay generation; s5.2 is now closed and BOTH GPU slots carry this substage's prerequisites: 62d88386 is the replay buffer at size (task f0f8801f, 8,000 prompts, L40S:1 aws, task defaults were already correct, hard prerequisite for C5a and C5b) and 2680e4a8 is the gating smoke on arm C4 (task 37186b22, -p arm=C4 -p smoke=true -p max_steps=30, L40S:1 aws), run in parallel rather than in the recorded serial order because both slots were free; queueing the smoke needed a task fix first, since s5-sft-sweep's registered parameter block was missing smoke, smoke_rows and max_steps even though main.py reads all three and the queue validator rejects unknown keys, and all three were added at exactly main.py's own defaults (false/512/0) so the eight full arms are unaffected; next, if the smoke passes, the arms go out two at a time in the recorded order C1 and C7, then C2', C4, C6, C3, then C5a and C5b once the buffer exists, at 64.0M tokens and 4.45 GPU-h each for 35.6 GPU-h total against 134 remaining; UPDATE 20:20Z: the C4 smoke PASSED (2680e4a8, 30 steps, 238,195 tokens, val loss 1.2552->0.3599, 2,833 tok/s, 6m25s), clearing all eight arms, and C1 (e4bd367a) and C7 (a68b3e57) are now running on both slots at 64.0M tokens each; the replay buffer FAILED on attempt 1 (62d88386) on the very defect the 17:09Z rewrite existed to prevent, arriving by a different door: generation was clean (7,945 completions, 0 empty, 1,850,696 completion tokens at 1,688.5 tok/s, placed in storage) but row_tokens took len() of apply_chat_template(tokenize=True), which on this transformers version returns a BatchEncoding whose len() is its KEY COUNT, so every row got n_tok=2 and a 1.85M-token buffer indexed as 15,890 training tokens; the downstream C5b ratio check read 201.4 passes against a 2.0 ceiling and failed the run, so no arm trained on it; fixed and re-uploaded three ways (an _ids() unwrap for BatchEncoding/dict/batched forms, an exact invariant asserting training tokens can never fall below the completion tokens the rows were built from so the cause is named at the point of failure rather than inferred, and an 8-case fixture test covering every return shape plus the len()==2 regression itself), requeue when a slot frees since C5a and C5b are last in the arm order and nothing is blocked; spend 11.339 of 145 approved GPU-hours; ARMS NOW PACK ONTO ONE CARD (2026-08-25 21:40Z, at the operator's direction and correcting my own error): the account's 2-GPU cap limits how many cards are up, not how much runs on them, and a 1.2B model with a rank-16 adapter uses a fraction of a 48 GB L40S, so one arm per card left most of both cards idle and made eight arms four sequential rounds; pack.py (task s5-sft-pack, cfc6dec7-cffb-40f2-ab9b-47dc12e0720c) runs main.py once per arm as a separate child process on one card, giving each a hard allocator ceiling of pack_headroom/n, its own out/<arm>/ namespace and a staggered start so four base-model loads do not stack their peaks, with children barred from the job API (the supervisor is the sole reporter, collecting each score.json and uploading artifacts <arm>__prefixed), the corpus fetched once per card instead of once per arm, and SIGTERM forwarded then escalated so a cancelled pack leaves nothing holding the GPU; comparability is enforced rather than argued, since a packed arm runs the same main.py from the same directory as a solo one (synced by sync_pack_task.sh) and C1 is still running solo as the control, while test_pack_isolation.py catches the two silent failure modes statically over main.py's syntax tree (an unguarded reporting call putting four writers on one stream, and a download outside the resolver pulling the corpus once per arm) with both mutation-tested, and test_pack_progress.py covers the seven ways the hours-long progress read goes wrong; peak GPU memory is now recorded on every run, packed or not, because that is the number that sizes the next pack; C7 was stopped at 484/9697 steps (0.794 GPU-h, charged) to free a slot for a four-arm packed trial c77ab752 carrying C2', C4, C6 and the full-parameter C3, since the pack size has to be chosen against the memory worst case rather than the median one, and C7 re-runs inside the first real pack; THE PACKED TRIAL RETURNED IN NINE MINUTES AND SETTLED THE SIZING (c77ab752, 0.153 GPU-h, charged): C2', C4 and C6 finished clean and C3 hit torch.OutOfMemoryError inside Adam's second-moment update with 9.4 GB still free on the card and its three siblings untouched, which is the per-process ceiling containing a failure exactly as designed and bought at nine minutes rather than at hour six of a fifteen-hour run; the numbers that size every later pack are that a LoRA r16 arm holds 7.52 GB as the driver and its siblings see it against 6.07 GB that torch reports allocated and 6.98 GB reserved, C6 at rank 64 holds 7.22 GiB, the entropy-weighted C4 holds 10.64 GiB and a full-parameter arm needs about 20-22 GB, so max_memory_allocated undercounts by roughly 1.5 GB per arm and pack_summary now sizes on reserved plus a 0.55 GB CUDA context allowance; aggregate throughput across three packed arms was 4,283 tok/s against 2,833 for one arm alone at the same step count, a 1.51x card-hour gain, and packed C4 consumed 238,195 tokens IDENTICAL BYTE FOR BYTE to the solo C4 smoke, which is the comparability proof; three robustness fixes followed (expandable_segments for children so fragmentation does not fail an arm that fits, a failing child's last forty console lines into the job log the moment it exits rather than at the end of the pack, and a stall_minutes watchdog so a hung arm cannot hold its slice of the card behind the wall clock); PACK A is running as 7e8ca5f9 (C3 + C7 + C2' at 22/9.5/9.5 GB under a 0.86 headroom, C3 first because full-parameter is the slowest arm, and C3 did NOT OOM at 22 GB) with C1 (e4bd367a) still solo beside it as the control that packing did not move the numbers; THE SUPERVISOR NOW ALSO SCHEDULES, so an arm need not be a trainer and need not start at the beginning: pack_scripts runs a named program per arm (replay.py is the self-distillation generator made packable the same way main.py was, sharing one stub, one storage() resolver and one exit-code contract, with the contract test now comparing the two files' preambles directly so drift fails locally), pack_after holds an arm until another exits clean, and pack_provides names the file that hands over so the supervisor registers out/<arm>/<file> as the local stand-in for the object the consumers would have downloaded and uploads it there once on their behalf; sizing moved to pack_gb in absolute gigabytes because relative shares charge a consumer for a producer it never coexists with, and the ceiling is checked against the pack's worst MOMENT (for each choice of which producers have finished, sum the arms alive in that state, take the largest) rather than the flat sum that would have refused a pack fitting at every instant; scheduling is the one part static checks cannot reach, since its failures all look like an ordinary exit code from outside, so test_pack_schedule.py runs the supervisor for real in about ten seconds against arm scripts that sleep for fractions of a second and a stand-in job API, covering the handover, a failed producer, a producer that exits clean having written nothing, peak sizing, an over-committed pack, per-arm scripts and four malformed-wiring cases, and mutating the supervisor to start every arm at once or to let consumers run after a failed producer fails it; PACK B is therefore the whole rest of the sweep as ONE job on ONE card (RB generating the buffer with replay.py, C4 and C6 training beside it from the start, C5a and C5b held until RB hands over, ceilings 18/13/9.5/9.5/9.5 GB peaking at 41.5 of a 42.9 GB limit AFTER the generator has released its 18 GB), which retires the stand-alone s5-selfdistill job from the critical path; it CANNOT be lodged in advance because the platform refuses a submission over the two-GPU limit rather than queueing it, so the exact command is written down as queue_pack_b.sh and goes out the moment C1 frees its slot around 00:30Z; spend 12.286 of 145 approved GPU-hours  The evaluation harness was made packable while the sweep ran: `jobs/s5-eval/main.py` is now a child under the same contract as the trainer, per-arm objects (each arm's checkpoint) are staged from the overrides, and the 4-bit server takes a per-arm port and scratch directory so two of them on one card cannot answer each other's requests. The contract test covers all three packable scripts. It is not registered as a task yet: it is the harness every recorded number came through, so it gets its own small two-arm packed run for a job id before `s6` leans on it. A stale replay buffer was found in shared storage under the name Pack B's RB arm writes: job 62d88386 generated it correctly but indexed every row at 2 tokens, and a consumer would have replayed 8,000 rows two hundred times while calling it replay. RB overwrites it, and the trainer now refuses any replay source whose mean row is under sixteen tokens. Check-in at 2026-08-26T12:45:00Z: Pack B (1df3bf2b) at 52% (C4 3570/8416, C6 3570/8416, C5a 3150/8429, C5b 3180/8481), Pass 2 (8b7bad6f) at 93%, both arms through BFCL and nearly done with ifeval/ifstruct (C2p 1776/2000, C7 1936/2000); both jobs progressing normally, no action needed. PASS 2 IS COMPLETE (8b7bad6f, rc=0, 2/2 arms, 2.541 GPU-h, all 30 artifacts verified present): C2p scored bfcl 0.7104/ifstruct 0.1391/ifeval 0.7488/probe_flag_rate 0.3889/probe_false_flag 0.0333, and C7 scored bfcl 0.7038/ifstruct 0.1458/ifeval 0.7411/probe_flag_rate 0.7222/probe_false_flag 0.0 — against C1's pass-1 baseline (bfcl 0.7196, ifstruct 0.1375, ifeval 0.7523, probe_flag_rate 0.6111), C7 IS THE FIRST ARM TO CLEAR ALL THREE PRE-REGISTERED s5.4 GATES: Gate 1 (detection >=0.70 AND false-flag <=0.15) passes at 0.7222/0.0; Gate 2 (no BFCL category >3.0pts below C1, IFEval no more than 2.0pts below) holds, both well inside tolerance; Gate 3 (IFStruct validity >= C1's 0.1375) passes at 0.1458. C2p (the no-guardrail ablation) fails Gate 1 as expected (flag rate 0.3889), confirming the guardrail block is doing real work rather than the base model incidentally flagging fabrications. Caveat recorded in runs/s5.3-sweep.md: this reading uses the 30-item synthetic clean arm per the 08:34Z gate amendment, since the 138-item corpus clean arm isn't wired into any arm's scoring yet. Scored files for C2p and C7 promoted into tidepool/s5.3/arms/{C2p,C7}/ via promote_scores.py (14 files, dry-run verified first, no clashes). budget.json's placeholder entry for 8b7bad6f updated in place to COMPLETE/2.541 GPU-h; spend now 33.724 of 145. Full detail, score table and gate-by-gate reading in runs/s5.3-sweep.md at 13:15Z. Check-in at 2026-08-26T13:15:00Z: Pack B (1df3bf2b) at 54% (C4 3780/8416, C6 3780/8416, C5a 3360/8429, C5b 3392/8481), active progress since last check-in, no stall, no action needed; one job now outstanding (1df3bf2b only). Check-in at 2026-08-26T13:28:00Z: Pack B (1df3bf2b) at 56% (C4 3990/8416, C6 3990/8416, C5a 3570/8429, C5b 3604/8481), active progress since last check-in, no stall, no action needed.
- [ ] s5.4 Direction decision with researcher  (checkpoint)  — THE DECISION RULE IS PRE-REGISTERED, written 2026-08-26 08:20Z while 385e210a was at 62% and no arm had a task score; it is in the s5 report under "The s5.4 decision rule, written before the numbers" and any departure will be recorded as one. Ranking metric: BFCLv3 overall, category-macro, better of the two calling conventions per arm, exactly as B1-B6 were scored, because that is the metric the pre-registered claim is written against. EVIDENCE GATE IS THE PAIRED TEST, NOT THE RANKING METRIC: an arm beats C1 only if its item-weighted paired delta survives Holm correction across the whole s5-compare family, and where the macro composite and the paired delta DISAGREE IN SIGN both are reported and no winner is claimed on that axis. Three gates first: (1) detection >= 0.70 with false-flag <= 0.15 on the clean items -- AMENDED 08:34Z on a configuration fact, before any arm had a score: clean_control_object is empty in pack.yaml and no arm queue script sets it, so NO ARM IS SCORED AGAINST THE 138-ITEM CORPUS CLEAN ARM. Not fixed by setting it for later passes, because C1 would then have no corpus cell for any arm to pair against; all eight arms stay identical and gate 1 is evaluated on the 30-item synthetic arm, which bounds the false-flag RATE only to about one item in thirty but does support the paired comparison s5.4 needs (an arm flagging a fifth of clean returns puts six discordant pairs against C1's zero). A probes-only pass with clean_control_object set is queued for every arm after the four scoring passes and before s5.5, 138 extra items an arm and a few card-minutes for all eight, and THE RESCOPE TRIGGER IS READ AGAINST THAT, not against the thirty; (2) no BFCLv3 category more than 3.0 below the matched base and IFEval no more than 2.0 below; (3) IFStruct at or above C1's. IF NOTHING SEPARATES (the likely outcome, since three of four arms tied within 0.0006 on val_loss) s5.4 does NOT pick the numerically highest arm: it picks the cheapest recipe passing all three gates, ranked r16 > r64 > full-parameter, no replay > 1% > 5%, reference mixture > raw, and records that the choice was made on cost because the arms were indistinguishable. A recorded non-separation is a finding about the sweep and goes in the paper as one. RESCOPE TRIGGER: if EVERY arm fails gate 1, the guardrail axis is not reachable from this training set, s5.5 has no direction worth detailing, and that stops for the operator rather than being decided under autonomous mode.
- [ ] s5.5 Detailed runs
- [ ] s5.6 Final tuning + ensembling

## s6 - Evaluation & analysis
- [ ] s6.1 Held-out eval vs. success criteria
- [ ] s6.2 Subgroup / fairness analysis
- [ ] s6.3 Calibration analysis
- [ ] s6.4 Error analysis
- [ ] s6.5 Robustness / ablations
- [ ] s6.6 Story checkpoint: agree the paper's story  (input checkpoint)

## s7 - Reporting & delivery
- [ ] s7.1 Academic paper (LaTeX → rendered PDF)
- [ ] s7.2 Model card
- [ ] s7.3 Reproducibility packages (core kit + chosen artifacts)  (input checkpoint)
- [ ] s7.4 Final sign-off  (checkpoint)
