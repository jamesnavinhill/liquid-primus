{
 "substage": "s5.3",
 "experiment": "tidepool",
 "jobs": [
  "e4bd367a-8148-4704-88a4-33e15636fa80",
  "7e8ca5f9-82a2-410e-8fda-ea53f5662bd2"
 ],
 "created": "2026-08-25T19:40:00Z",
 "updated": "2026-08-25T22:10:00Z",
 "note": "PACKING IS LIVE AND MEASURED. Task s5-sft-pack = cfc6dec7-cffb-40f2-ab9b-47dc12e0720c (queue by FULL UUID; short ids do NOT resolve for `task queue` and it fails with a misleading 'Task has no parameters declared'). Regenerate its dir with ./sync_pack_task.sh in the s5-sft-sweep job dir, then `lab task edit <uuid> -e tidepool --from-dir /tmp/s5-sft-pack --no-interactive`. Task edits do NOT affect already-launched jobs (confirmed: the smoke ran its queue-time snapshot), so editing mid-run is safe. MEASURED FOOTPRINTS from the packed smoke c77ab752, use these to size every pack: as the DRIVER sees them, a LoRA r16 arm is 7.5 GB and the entropy-weighted arm C4 is 11.4 GB; torch's max_memory_allocated UNDERCOUNTS by about 1.5 GB per arm (6.07 allocated vs 6.98 reserved vs 7.52 actual), so size on reserved + 0.55 GB of CUDA context, which pack.py now does. Card is 47.7 GB decimal (44.4 GiB). A full-parameter arm (C3) needs about 22 GB: it OOMed at Adam's exp_avg_sq step inside an 11.0 GB ceiling while 9.4 GB was still free on the card, and its three siblings finished normally -- the isolation works. Packed C4 saw 238,195 tokens, IDENTICAL to the solo C4 smoke, which is the comparability proof. Aggregate throughput for 3 packed arms was 4,283 tok/s against 2,833 for one arm alone at the same step count, i.e. about 1.5x per card, so packing buys roughly a third off the sweep's card-hours. NOW RUNNING, both slots full: e4bd367a is arm C1 SOLO (reference recipe AND the control that packing did not move the numbers; ~1890/8416 steps at 5,224 tok/s, terminal ~00:30Z) and 7e8ca5f9 is PACK A = C3, C7, C2p on one card (weights 22,9.5,9.5, headroom 0.86, stagger 60s, full 64M-token budget each). C3 is in pack A because full-parameter is the slowest arm and should start earliest. NEXT, when a slot frees: run queue_pack_b.sh. Pack B is now ONE job that finishes the sweep: RB runs the self-distillation generator (replay.py) as an arm, C4 and C6 train beside it, and C5a/C5b are held by pack_after until RB exits clean, then read its buffer off local disk while the supervisor uploads it to tidepool/s5.3/replay/replay.jsonl.gz once. The separate s5-selfdistill job (task f0f8801f-f4e9-4f21-94c2-1323257736b5) is NO LONGER NEEDED unless Pack B's RB arm fails. That completes all eight arms plus the buffer in one card-run. QUEUE HYGIENE: always --provider aws --no-interactive and the FULL task UUID. FILLER WORK, zero dependencies: supplementary re-scores of B1, B2, B3, B4 over the 138-item clean-control arm (task 7633fb1c-f530-42b4-9556-10c9f5f729f1); any eval queue needs all five zero limits (-p limit_per_component=0 -p limit_bfcl=0 -p limit_ifstruct=0 -p limit_ifeval=0 -p limit_probes=0) plus -p clean_control_object=tidepool/s5.3/tooldata/clean_control.jsonl. s5-eval's FP path generates in-process (gen.py), not via a server, so the SAME pack pattern applies to evals and is worth building before the s6 wave of 8 arms x 4 components. OPERATOR STEER (2026-08-25 21:05Z): a 2-GPU cap is not a cap on parallel work; pack each card as full as it fits and engineer the isolation. Spend 12.286 of 145 approved GPU-hours.",
 "next_action": "Run stages/s5-experimentation/jobs/s5-sft-sweep/queue_pack_b.sh as soon as job e4bd367a (arm C1) finishes, which frees the second of two GPU slots. The platform REFUSES a submission over the limit rather than queueing it, so Pack B cannot be lodged in advance; the script exists so the submission goes out unchanged the moment the slot opens. C1 was ~1890/8416 steps at 5,224 tok/s at 21:40Z, so it lands around 00:30Z.",
 "notes": " | The s5-eval task is NOT synced to the packing-aware code in the source tree. Register it only after a two-arm packed eval smoke has passed on a real job; until then any eval queued against task 7633fb1c runs the old solo harness, which is the intended behaviour."
}