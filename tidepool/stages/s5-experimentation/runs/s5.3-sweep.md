# s5.3 — the supervised sweep

The C row of the experiment matrix, authored as one task and queued one arm at a time.
Task `s5-sft-sweep`, id `37186b22-9fbc-4c63-8448-4a2d21d64384`, source in
`../jobs/s5-sft-sweep/`.

## What differs between arms, and nothing else

Every arm runs the same file. The only thing that changes at queue time is `-p arm=<id>`,
and each arm's delta from the reference lives in one table in `main.py`, so the sweep's
specification is a block of code rather than eight command lines that can drift apart.

| Arm | Delta from C1 | What it isolates |
| --- | --- | --- |
| **C1** | reference: LoRA r16 α32, LR 1e-4, role-balanced mix, cross-entropy, guardrail block at 2 passes | the recipe everything else is measured against |
| **C2'** | `guardrail_epochs: 0` | what the guardrail supervision buys on the probe flag rate |
| **C3** | `tuning: full`, LR 1e-5, micro-batch 1 × accumulation 16 | adapter against full-parameter |
| **C4** | `loss: entropy_weighted` | loss shaping on the guardrail and tool axes |
| **C5a** | `replay_frac: 0.01` | 1% self-distillation replay |
| **C5b** | `replay_frac: 0.05` | 5% self-distillation replay |
| **C6** | `lora_r: 64`, `lora_alpha: 128` | adapter capacity |
| **C7** | `mix: raw` | what role balancing buys, against uniform sampling |

`C2` as originally written ("public data only") was retired at `s5.2` and its cell reassigned;
the reasoning is in `../../s3-research-plan/plan.md`. `C5` was specified over three replay
fractions, and 0% is C1 itself, so it costs two cells rather than three. Eight arms total.

Three arms change two things at once, and each does so deliberately.

- **C3** moves the learning rate with the tuning method. LoRA and full-parameter fine-tuning
  have different optimal LR scales by construction, and holding 1e-4 across both would compare
  a tuned recipe against a detuned one. Its micro-batch drops to 1 and accumulation rises to
  16, so the effective batch stays at 16 while the optimizer states for 1.2B parameters fit
  beside the activations on the same card.
- **C6** moves α with r, keeping α/r at 2. Rank is the variable; the scaling factor is not.
- **C2'** returns the guardrail tokens to the base mix rather than shrinking the run, so the
  comparison is the guardrail block against an equal weight of ordinary supervision.

## Equal cost, enforced

Each arm trains on exactly **64,000,000 tokens**, which is 0.71 of an epoch over the
calibrated mix and **4.45 GPU-hours** at the 3,994.2 tok/s measured on an L40S at `s5.1`.
Eight arms is **35.6 GPU-hours**. The run asserts its own token count at the end and fails
the arm if it lands under 90% of budget, because an arm that quietly trained on less is an
arm whose result reads as a hyperparameter effect and is a compute effect.

The `s5.1` calibration derived the budget from the mix constraints, which was right for a
calibration and wrong for a sweep. The constraints now set the *shares* and the budget is
fixed, so the two roles with fixed doses come out of the budget rather than on top of it:

| Role | Dose | Tokens | Share of budget | Passes over the source |
| --- | --- | ---: | ---: | ---: |
| Tool calling | scaled | 24,900,000 | 38.9% | 0.61 |
| Guardrail block | 2 passes, fixed | 8,703,580 | 13.6% | 2.00 |
| SQL | scaled | 15,142,000 | 23.7% | 0.17 |
| Code | scaled | 12,506,000 | 19.5% | 0.17 |
| Structured output | scaled, capped at 3 passes | 2,746,000 | 4.3% | 1.83 |

Priority roles take **56.8%** against a 50% floor. C5a and C5b take their replay off the top
(0.64M and 3.2M tokens), which pulls the base roles down proportionally and leaves the total
unchanged.

**Why two passes over the guardrail block.** The block is 7,988 rows and 4.35M tokens, and the
amended gate asks the probe flag rate to move from 0.0074 to 0.35. Dosing it by share would
make its weight an artifact of how large the rest of the corpus happens to be, so it is dosed
in passes like structured output, which the same argument already allows three. Two sits under
that precedent and lands at 13.6% of the budget. The dose is the first knob at `s5.4` if the
flag rate falls short, and `C2'` is what tells us whether the block is doing anything at all.

## The entropy-weighted loss, and the trap it avoids

C4 weights each supervised token by `1 + β·(H − H̄)`, where `H` is the model's own normalized
predictive entropy at that position and `H̄` is the mean over the supervised tokens in the
micro-batch, clamped at 0.1 below. The centring is the whole point: it holds the mean weight at
1, so `β` cannot act as a hidden learning-rate multiplier and the arm measures loss shaping.
Entropy is computed under `no_grad` in chunks over the flattened token axis, because a float32
softmax over the full vocabulary for a whole micro-batch is a gigabyte-scale temporary.

Every arm is scored on **plain cross-entropy over the held-out split**, never on its own
training objective. C4 reweights what it optimizes, and an arm ranked on its own reweighted
loss cannot be compared to the others.

## What has to exist before every arm can run

**The replay set does not exist.** C5a and C5b are the only arms that need it. Task
`s5-selfdistill` (`f0f8801f`) passed its `s5.1` smoke at 64 prompts and has never been run at
size; the prompt pool it draws from is `tidepool/s4.4/prompt_pool.jsonl.gz`, 447,053 rows.
C5b needs 3.2M replay tokens, so the generation run is sized at roughly 8,000 prompts. Until
it lands, an arm queued with `replay_frac > 0` and no `replay_object` **fails loudly** rather
than silently training C1 under a C5 label.

## Local verification

`test_sampler.py` runs on CPU over gzip fixtures in about a second and covers the three ways
this sweep could be quietly invalid: arms costing different amounts, the guardrail-off arm
receiving guardrail rows or the guardrail-on arms receiving none, and selection order drifting
between arms. **25 checks, all passing**, including:

- every arm lands within 0.1% of the 64.0M budget, on the real measured corpus sizes
- C1 sees the guardrail block exactly twice and C2' sees none of it
- C2' returns the freed tokens to the base mix instead of shrinking the run
- a replay arm with no replay rows is flagged rather than run as C1
- a budget smaller than the fixed doses is flagged rather than silently starving the mix
- two arms differing only in a hyperparameter draw identical base rows in identical order
- centring holds the mean entropy weight at 1.0000 at β ∈ {0.5, 1.0, 2.0} while still spreading
  individual weights from 0.23 to 2.09

## Queue order

The account allows two GPUs at a time and both are occupied by `s5.2` rows. As they free:

1. the four remaining baseline evals, B1r first
2. a smoke of this task at `smoke: true, max_steps: 30`, on arm C4, which exercises the new
   multi-source sampler, the guardrail role and the entropy loss in one cheap run
3. the replay generation at size, alongside the first sweep arm
4. C1 and C7 first, then C2', C4, C6, C3, then C5a and C5b once replay exists

**No `sweeps:` block, deliberately.** A `sweep_config` fans out inside a single job. Two GPUs
at a time and 4.45 GPU-hours per arm would make that one job run for a day and a half, a single
failure would take the whole C row, and the ledger would lose the per-arm job ids it is written
against. Individual queues cost one extra command each and keep all three.

## The replay buffer, and the silent failure it was one field away from

`C5a` and `C5b` are the only arms with a prerequisite outside the sweep task. Both replay
self-distilled completions over the general-instruction prompt pool, and that buffer did not
exist: the generator had only ever run a 64-prompt smoke at `s5.1`.

Sizing it is arithmetic. The smoke produced 245 completion tokens per prompt, greedy, at 320
new tokens maximum. `C5b` replays 5% of a 64.0M-token budget, or 3.2M tokens, and a training
row costs its prompt as well as its completion, so 8,000 prompts land near 4M training tokens
and `C5b` makes a little under one pass over the buffer. The run asserts on that ratio rather
than trusting the estimate: if the buffer came back small enough for `C5b` to replay it more
than twice, the arm would be measuring memorization of a small set as much as it measures
replay, and the run fails instead of producing a number that looks like replay and is not.

The sample is drawn by hash rank against a fixed salt, so 8,000 prompts is a strict superset
of the smoke's 64, and those 64 have already been generated twice with byte-identical output.
Greedy decoding and one generation for the whole sweep are what make the buffer a constant
rather than a source of variance in the comparison it exists to stabilize.

**The defect worth recording is the one that would not have announced itself.** The sweep's
sampler reads every source through a single code path: rows are keyed by `role`, each role is
sized from an `n_tok` field, and a file is opened by its suffix. The replay writer emitted
neither. A buffer with no `n_tok` indexes as **zero available tokens**, and a role with zero
available tokens contributes nothing to the mix, so both replay arms would have trained the
reference recipe, reported a replay fraction of 1% and 5% in their run records, and produced
the result that replay changes nothing. Every guardrail in the sweep would have passed. The
writer now counts tokens with the training tokenizer and the training template, writes
`replay.jsonl.gz`, and places the file at `tidepool/s5.3/replay/`, because the arms load
replay from shared storage rather than from a job artifact. The generating run fails if the
placement fails.

Card: an L40S rather than the smoke's L4, chosen on throughput, pinned to aws per the
provider note. The run is priced at well under an hour.

## The gate passes, and the replay buffer fails on the defect it was rewritten to prevent

Both `s5.2` slots freed at once when `B4` and `B5` landed together, so the smoke and the replay
generation went out in parallel rather than in the serial order recorded above. The smoke was
also what forced a task fix: `s5-sft-sweep`'s registered `parameters:` block did not list `smoke`,
`smoke_rows` or `max_steps`, although `main.py` reads all three, and the queue validator rejects
unknown keys outright. All three were added at exactly `main.py`'s own defaults (`false`, `512`,
`0`), so the eight full arms are byte-identical to what was specified.

**The gate passed.** `2680e4a8`, arm `C4`, 30 steps over 238,195 tokens in 6m25s at 2,833 tok/s,
val loss 1.2552 → 0.3599. The multi-source sampler, the `tool_guardrail` role and the
entropy-weighted loss all ran together without incident. All eight arms are cleared.

**The replay buffer failed, and the assertion that caught it earned its place.** `62d88386`
generated cleanly: 7,945 of 8,000 sampled prompts usable inside the 1,024-token cap, 7,945
completions, 0 empty, **1,850,696 completion tokens** at 1,688.5 tok/s, file placed in storage.
Then it failed one check, because the `n_tok` written into every row was **2**.

The cause is one call. `row_tokens` did `len(tok.apply_chat_template(msgs, tokenize=True, ...))`.
On this `transformers` version that call returns a `BatchEncoding`, and `len()` of a
`BatchEncoding` is its **key count** — `input_ids` and `attention_mask` — not its token count. So
a 1.85M-token buffer indexed as 15,890 training tokens.

This is the same defect this generator was rewritten at 17:09Z to prevent, arriving through a
different door. The rewrite made sure `n_tok` was *written*; it did not make sure the number
written was *a token count*. What saved it is that the earlier reasoning also left a downstream
check with independent evidence behind it: `C5b`'s 3.2M-token dose over a 0.02M-token buffer reads
as **201.4 passes** against a 2.0 ceiling, so the run failed and no arm ever trained on it.

Three changes went up together:

- `_ids()` unwraps `BatchEncoding`, plain `dict`, and batched list-of-lists forms before counting.
- **An exact invariant**, not a heuristic: a training row carries its prompt *and* its completion,
  so the buffer's training-token total can never fall below the completion tokens it was built
  from. This run violated that by two orders of magnitude. The ratio check caught the symptom;
  this names the cause at the point of failure instead of leaving it to be inferred.
- `test_row_tokens.py`, 8 cases, covering every return shape and asserting the `len() == 2`
  regression itself still reads 2 so the test cannot quietly stop testing anything.

`C5a` and `C5b` are last in the arm order, so nothing is blocked. The requeue goes out when a slot
frees. 0.372 GPU-h was spent and is recorded: the generation really ran.

## Arms C1 and C7 are away

Both slots went to the first two arms as soon as the smoke cleared: `e4bd367a` (`C1`, the
reference recipe) and `a68b3e57` (`C7`, `mix: raw`). L40S:1 each, pinned to aws, 64.0M tokens and
about 4.45 GPU-h apiece. Remaining order: `C2'`, `C4`, `C6`, `C3`, then `C5a` and `C5b` once the
buffer exists. Project spend is **11.339 of 145 approved GPU-hours**.

## Arms now share a GPU

*2026-08-25 21:40Z*

The operator pushed back, correctly, on running one arm per card. An L40S is 48 GB; a
1.2B model with a rank-16 adapter uses a fraction of it. Treating the account's two-GPU
cap as a cap on *parallel work* was my error: it caps how many cards are up, not how much
runs on them. Eight arms at one per card is four sequential rounds and most of both cards
idle throughout.

`pack.py` is the supervisor that fixes it. It runs `main.py` once per arm as a separate
child process on one card, which puts the isolation in the operating system rather than in
a convention:

| concern | how a packed arm is isolated |
|---|---|
| process | its own `python main.py`; an OOM, an assertion or a kill takes that arm alone |
| memory | a hard allocator ceiling of `pack_headroom / n`, so an arm growing into its neighbours hits its own limit first |
| filesystem | writes only to `out/<arm>/`; no two arms can touch one path |
| reporting | children never call the job API; the supervisor collects each `score.json` and uploads artifacts `<arm>__`-prefixed |
| inputs | the corpus is downloaded once per card, not once per arm |
| shutdown | SIGTERM forwarded to every child, escalated to SIGKILL after a grace period |

Starts are staggered by 30–45 s. Every child peaks its memory while loading the same base
model, so starting four together stacks four load peaks that the steady state never
reaches — the pack could die on a transient that the run it is trying to do never touches.

**Comparability is the thing packing must not cost, so it is enforced rather than argued.**
A packed arm runs the same `main.py` a solo arm runs, from the same directory, copied by
`sync_pack_task.sh`; the only differences are where it writes, that it takes its config off
the environment, and that it exits non-zero instead of reporting. Arm C1 ran solo before
packing existed and is still running solo now, which makes it the control: if packing moved
the numbers, C1-solo against a packed arm's tokens-seen, priority share and row order is
where it would show.

Two failure modes were easy to introduce and invisible once introduced, so both are caught
statically by `test_pack_isolation.py` over `main.py`'s own syntax tree — one unguarded
`lab.log` puts four writers on one progress stream, one unguarded `lab.storage_download`
pulls the corpus once per arm. The stub a child gets in place of the job API answers
reporting calls as no-ops but *refuses* capability probes, because
`getattr(lab, "storage_upload", None)` coming back as a no-op would have the caller report
a successful upload of nothing. Both checks were mutation-tested: breaking each rule on
purpose fails the test.

Runs now also record peak GPU memory whether packed or not, which is the number that sizes
the next pack.

### Cost of finding out

Arm C7 was stopped at 484/9697 steps to free a slot for a four-arm packed smoke
(`c77ab752`: C2', C4, C6 and C3 at 30 steps each, profiling on, weights to an `-smoke`
prefix so no real arm is overwritten). C3 is in it deliberately: full-parameter tuning is
the memory worst case, and the pack size has to be chosen against the worst arm rather than
the median one. The stop cost 0.794 GPU-hours, charged to the ledger; C7 re-runs inside the
first real pack. The alternative was to wait about three hours for a slot and then commit
the remaining arms to supervisor code that had never executed.

### What the smoke measured

The four-arm smoke returned in nine minutes and settled every open question about sizing.

Three arms finished clean and one failed, which is the result worth having. C3 hit
`torch.OutOfMemoryError` inside Adam's second-moment update while **9.4 GB was still free on
the card**: its own per-process ceiling stopped it, its three siblings never noticed, and the
supervisor reported a partial pack with the failing arm named and its last forty console
lines in the job log. That is the isolation doing exactly the job it was built for, bought
for 0.15 GPU-hours instead of at hour six of a 900-minute run.

| | torch says allocated | torch says reserved | driver and siblings see |
|---|---|---|---|
| LoRA r16 arm | 6.07 GB | 6.98 GB | **7.52 GB** |
| LoRA r64 (C6) | — | — | **7.22 GiB** |
| entropy-weighted (C4) | — | — | **10.64 GiB** |
| full-parameter (C3) | — | — | **~20–22 GB** (OOMed inside 11.0) |

`max_memory_allocated` undercounts by about 1.5 GB per arm, so sizing on it overcommits a
card by 4–6 GB across a pack of four. `pack_summary.json` now sizes on
`max_memory_reserved` plus a 0.55 GB CUDA context allowance, which reproduces the observed
footprint to within a tenth of a gigabyte.

Two numbers justify the whole exercise. Aggregate throughput across three packed arms was
**4,283 tok/s against 2,833 tok/s for a single arm alone** at the same step count — 1.51×
per card, so the sweep costs about a third fewer card-hours and leaves the second slot free
for other work. And packed C4 consumed **238,195 tokens, identical byte for byte to the solo
C4 smoke**: same rows, same order, same priority share. Packing changed the schedule, not the
science.

Pack A (`7e8ca5f9`) is C3 + C7 + C2' on one L40S at 22 / 9.5 / 9.5 GB under a 0.86 headroom,
C3 first because full-parameter is the slowest arm and should start earliest. C1 stays solo
as the control. Pack B is C4 + C6 + C5a + C5b at 13 / 9.5 / 9.5 / 9.5, once the replay buffer
the C5 pair reads exists.

### An arm does not have to be a trainer

Two of the eight arms replay a buffer of the base model's own frozen answers, and that buffer
is generated by a different program. Run as its own job it is a card at a fifth of its
capacity for forty minutes, followed by a second queue-and-launch cycle before the two arms
that need it can start; and the account refuses a third GPU rather than queueing it, so the
two cycles are strictly serial.

The supervisor now takes three more parameters that turn that into one card-run:

- `pack_scripts` — an arm runs a named program rather than the sweep trainer. `replay.py` is
  the self-distillation generator, made packable the same way `main.py` was: same stub for the
  job API, same `storage()` resolver, same exit-code contract. Its generation path is
  untouched, and the contract test now compares the two files' packing preambles directly, so
  an edit to one that is not made to the other fails locally.
- `pack_after` — an arm is held until another exits clean. One level deep, deliberately: two
  waves cover producer-then-consumer, and a real scheduler inside a supervisor whose value is
  that it is simple to reason about is a bad trade.
- `pack_provides` — names the file that hands over. When the producer exits clean the
  supervisor registers `out/<arm>/<file>` as the local stand-in for the object its consumers
  were going to download, and uploads it there once, on their behalf. The consumers read local
  disk; shared storage still ends up with the buffer for every later stage.

Sizing had to change with it. Relative shares divide the card between every arm listed,
including arms that never coexist, so a consumer still pays for the producer it replaced.
`pack_gb` states each ceiling in gigabytes — the unit the measurements arrive in and the unit
the failures arrive in — and the ceiling is checked against the pack's worst *moment* rather
than the flat sum: for each choice of which producers have already finished, add up the arms
alive in that state, and take the largest. Pack B peaks at 41.5 GB of a 42.9 GB limit, and it
peaks after the generator has handed over, which is exactly what the flat sum would have
refused.

**Scheduling is the one part of this that static checks cannot reach**, because it is a loop
over live processes and its failures all look like an ordinary exit code from outside: an arm
that never starts, an arm that starts before its input exists, a consumer that runs on a stale
file after its producer failed. So `test_pack_schedule.py` runs the supervisor for real
against arm scripts that sleep for fractions of a second and a stand-in for the job API, and
checks the handover, a failed producer, a producer that exits clean having written nothing,
peak sizing, an over-committed pack, per-arm scripts and four malformed-wiring cases. It takes
about ten seconds and needs no GPU. Mutating the supervisor to start every arm at once, or to
let consumers run after a failed producer, fails it.

Pack B is therefore the whole rest of the sweep as one job on one card: `RB` generating the
buffer, `C4` and `C6` training beside it from the start, and `C5a` and `C5b` starting when the
buffer exists. The stand-alone `s5-selfdistill` job is no longer on the critical path.

One further parameter exists for the stage after this one. The sweep trainer recognises its
own arm — `arm` selects a recipe from a table inside `main.py`, so one shared config describes
all eight arms — but an evaluation arm is a checkpoint and a component rather than a name in a
table, and s6 is eight arms across four components. `pack_overrides` takes JSON and patches the
shared config for one arm only, which is how a future child script is configured without an
arm table of its own. It is JSON rather than a bare string so that values keep their types; a
string parameter turns every number into text and every `false` into a `true`. Mutating the
supervisor to apply every arm's patch to every arm fails the scheduling test.

### The evaluation harness now packs too

Scoring is where the same waste shows up next, and worse. `s6` puts eight checkpoints through
four components; each pass alternates a generation batch with a stretch of CPU-bound grading,
so a card running one of them is idle for a good part of its own job. Eight sequential eval
jobs would be eight card-hours to do maybe three cards' worth of work.

`jobs/s5-eval/main.py` is now a packable child under the same contract as the trainer and the
replay generator, and the contract test reads all three rather than the two in this directory.
Three things had to change that the trainer never exposed, all of them the same failure in
different clothes: a shared default that two children would fight over.

**Arms differ in their inputs.** Every sweep arm reads one corpus; every eval arm reads a
different checkpoint. An adapter arrives in `pack_overrides`, so the supervisor's input
staging now walks the per-arm patches as well as the shared config, and it finds objects by
the project's own naming rule (any config key ending in `_object`) rather than a list written
out in `pack.py` that a new input would quietly fall off. Objects two arms share are still
fetched once. An object no arm on the card actually reads is no longer fatal: the arm that
needs it says which one it was denied, which is a better error than the supervisor guessing.

**The 4-bit path serves on a port.** `llama.cpp` runs as its own process listening on 8080.
Two of those on one card is not an out-of-memory error, which would at least be loud: the
second server fails to bind and the first answers both arms, and the run produces two
plausible-looking score sets from one model. Each child now gets its position in the pack on
the environment and derives its port from it, and the backend, the weights and the server log
go under the arm's own directory instead of three shared ones. The contract test fails if
either the port or the directories go back to being fixed.

**A memory ceiling only binds what allocates through torch.** The full-precision path does,
so it takes the same per-process ceiling the trainers use. The 4-bit server does not, and
pretending otherwise would be the dangerous kind of wrong: a pack carrying one sizes it by
its context length and slot count and records in the summary that that is what happened.

The changed harness is deliberately **not** registered as a task yet. It is the most reused
artifact in the project, every number in the comparison table came through it, and a solo
rerun of it is what most of `s5` is built on. It gets a small two-arm packed run of its own
for a job id before anything depends on it, on the first slot that frees after the sweep.

### A buffer in storage that must not be used

Shared storage already holds `tidepool/s5.3/replay/replay.jsonl.gz`, and it is the wrong
file. Job `62d88386` generated it: 7,945 self-distilled completions, 1.85M generated tokens,
none empty, uploaded, and then the job failed its own assertion. The generation was fine. The
accounting was not: every row was written with `n_tok = 2`, because `apply_chat_template`
returns a `BatchEncoding` on this version and `len()` over one counts its two keys rather than
its ids. The file therefore indexes a 1.85M-token corpus as 15,890 tokens.

The generator has been fixed since (it unwraps the encoding, and it now refuses to finish if
its per-row token count is smaller than the tokens it just generated), so `RB` in Pack B
rebuilds the object correctly and overwrites it. What was missing is a guard on the other
side. Nothing about the bad file looks wrong to a consumer: the rows are tagged `replay`, so
the "no replay rows" check passes, and the sampler simply asks for a 3.2M-token dose from a
set it believes holds 15,890 — which it satisfies by replaying the same 8,000 rows two hundred
times. C5b would have run to completion and reported a number measuring memorization of a
small set, under the name replay.

`main.py` now checks the mean tokens per row of any replay source it is given and fails the
arm if it is under sixteen, naming the object. A self-distilled row is a couple of hundred
tokens; a single-digit mean is a field counting something other than tokens. The stale object
is left where it is rather than deleted: the guard is what makes it safe, and a guard is worth
more than a clean bucket, since the next miscount will not be this one.

## The reference arm's second attempt never got a machine (2026-08-26 03:44Z)

Arm `C1` was re-queued at 00:55Z as a single-arm pack (`5d9be511`, task `cfc6dec7`,
`arms=C1 pack_gb=16 stall_minutes=45 run_tag=s5.3-C1`) after attempt 1 stalled and was
stopped. It never ran. AWS refused `RunInstances` in `eu-central-1a`:

    PendingVerification: Your request for accessing resources in this region is being
    validated, and you will not be able to launch additional resources in this region
    until the validation is complete.

That is an account-region validation on the AWS account Transformer Lab runs on. It is
not capacity, not the team's 2-GPU cap, and not the task: the job reported `progress 0`,
`launch_progress.phase = failed`, no `resources` block and no artifacts, and it is charged
at **0.000 GPU-h** — a measured zero, not an unknown.

**Why the fix was to change provider rather than to retry.** The provider steering note
(`updated_at 2026-08-23T22:48:37Z`) says two things that settle it. AWS GPU quota exists
**only** in `us-east-1` and `us-east-2`, and every other AWS region is zero; and the L40S
source row is **AWS (g6e), Nebius, RunPod, in try order**, to be worked left to right
without stopping before the last entry. The launch had landed in a region that has no
quota to begin with, and the region is not a knob `lab task queue` exposes — there is
`--provider`, and no `--region`. Retrying AWS unchanged would have been a coin flip on
which region the scheduler picked next. Working the row is the documented move, so `C1`
went to **Nebius**, the second entry.

Attempt 3 is job `1728ed4a-ff04-4cdf-b1dc-c05f5df05de2`, queued 03:46Z on `nebius` with
the recipe unchanged in every parameter. It reached `launching_cluster` inside a minute,
past the point attempt 2 died.

**What this does to comparability.** `C1` is the reference every other arm is measured
against, and it is now the only arm that will have run on a different provider from its
siblings. The card is the same model either way — L40S:1, 48 GB — and the recipe,
tokenizer, seed and 64.0M-token budget are byte-identical, so the comparison is between
two runs of the same code on the same card from two vendors. That is a weaker guarantee
than the one packing has (packed `C4` consumed exactly the solo `C4` smoke's 238,195
tokens), and it is worth stating rather than assuming: if `C1`'s throughput or loss curve
sits oddly against the packed arms, the provider is a candidate explanation and re-running
it on AWS once the region clears is the check. The provider note's own narrow exception
points the same way — a number to be compared directly against numbers on specific
hardware should not silently change hardware. Here the hardware model does not change,
only who rents it, so the run proceeds and the caveat is recorded.

**Relaunch accounting for `s5.3`/`C1`:** three attempts, each with a distinct named cause
and a distinct fix — a silent stall with no identified cause (stopped, and answered with
checkpointing plus the watchdog), a launch refused by an account-region validation
(answered by moving to the next source in the card's row), and the attempt now running.
Two of the three are progress by the rule that matters: the failure signature changed each
time. Nothing about the experiment design has been weakened to get a job through.

### Nebius has its own ceiling, and it is not ours

If a later Nebius launch is refused with `ResourceExhausted ... compute.instance.count
(limit N, requested N+1)`, that is the account-wide cap Nebius applies across every
Transformer Lab tenant. It costs nothing, it clears when other tenants release instances,
and the response is to move to **RunPod**, the third entry in the L40S row, rather than to
retry Nebius unchanged.

## The reference arm completes (2026-08-26 06:52Z)

Attempt 3 (`1728ed4a`) ran clean to the end on Nebius: 8,416/8,416 steps, 65,576,421 tokens
seen at 6,094.3 tok/s, `val_loss` **0.1517** (a delta of -1.1035 from the base checkpoint),
zero assertion failures, peak 6.07 GB on a 47.7 GB card. All six artifacts uploaded
(adapter, console log, metrics, mix calibration, score, train history) plus
`pack_summary.json`. Billed at 3.001 GPU-h — under the 4.45 GPU-h estimate, consistent with
the packed arms' throughput rather than anything provider-specific. `C1` is `s5.3`'s
reference recipe (LoRA r16, `role_balanced` mix, plain cross-entropy), so every other arm's
result is now read against this number.

The loss curve gives no reason to reopen the AWS-vs-Nebius comparability caveat above: the
throughput (6,094 tok/s) and memory footprint (6.07 GB) sit in the same range packed LoRA
arms have shown on AWS L40S cards (7.52 GB driver-side, similar token rates), so nothing
about this run looks like a vendor artifact. The caveat stays recorded rather than retired,
since the only real test — the same recipe on AWS once the region clears — hasn't run and
isn't planned unless a later number looks odd against it.

Three relaunch attempts, three distinct causes, three distinct fixes (an unidentified silent
stall answered with checkpointing and a watchdog; a refused launch answered by moving
providers; this run). The reference arm is in hand at three attempts and 8.11 GPU-h total
across all three (4.059 stalled + 0.0 refused + 3.001 complete), against 4.45 GPU-h had it
run clean the first time.

Checkpointing itself was never exercised end to end: `lab storage ls tidepool/s5.3/ckpt`
stayed empty for the whole run, confirming the standing finding that the CLI-fallback
storage upload from inside a packed child is failing silently (see the pending-jobs record
for the diagnosis). It cost nothing here because the run never stalled, but the safety net
this relaunch exists to provide is unproven, and worth fixing in source before a job that
actually needs to resume from a stall meets it.

## Pack A completes: three arms on one card (2026-08-26 07:01Z)

`7e8ca5f9` finished after 9 h 16 m with `3/3 arms completed`. All three exited rc=0 at the
full 64.0M-token budget with zero assertion failures, and all 18 per-arm artifacts plus
`pack_summary.json` are listed by `lab job artifacts`, so every number below is backed by a
file rather than by a score dict alone.

| Arm | What it changes | `val_loss` | Δ vs base | Steps | tok/s | Peak GB |
| --- | --- | --- | --- | --- | --- | --- |
| **C1** (`1728ed4a`, solo, Nebius) | reference recipe | **0.1517** | −1.1035 | 8,416 | 6,094 | 6.07 |
| **C2'** | no guardrail block | **0.1511** | −1.1041 | 8,585 | 2,019 | 6.07 |
| **C3** | full-parameter, LR 1e-5 | **0.1513** | −1.1043 | 8,416 | 2,274 | 11.72 |
| **C7** | raw mix, no role balancing | **0.1565** | −1.0987 | 9,697 | 2,009 | 6.07 |

Four of eight arms are down. The remaining four (`C4`, `C6`, `C5a`, `C5b`) plus the replay
buffer they depend on are Pack B, queued as `1df3bf2b` the moment the card freed.

### The selection metric is flat, and that changes what `s5.4` can decide on

Three of the four arms sit inside 0.0006 of each other. At one seed per arm that is not a
ranking, it is a tie: full-parameter tuning of all 1.2B parameters lands within noise of a
rank-16 adapter, and removing the guardrail block entirely moves `val_loss` by 0.0006 in the
*better* direction. Only `C7` separates, and it is the one arm that changes the data mix:
role balancing is worth about 0.005 of held-out cross-entropy against uniform sampling, which
is the one clean effect in the table.

Two of those non-results are expected by construction and should not be read as surprises.
`C2'` returns the guardrail tokens to the base mix rather than shrinking the run, so it trains
on an equal weight of ordinary supervision — what the guardrail block buys was never going to
show up in general-mix cross-entropy, it shows up in the probe flag rate, which `s6` measures.
`C3` changes the optimizer's reach, not the data, and a 64.0M-token budget is small enough
that a rank-16 adapter is unlikely to be the binding constraint on fit.

What it does mean is concrete and it lands on the next substage: **the sweep's cheap
selection metric cannot pick a direction.** The `s5.4` decision has to rest on the task
metrics — native and text-convention tool calling, structured output, instruction following,
guardrail flag rate — scored by the `s5.2` harness over each arm's adapter. That harness is
the last unproven piece of the chain, which is why its packed form is being given a job id
now (`18c83b6b`) rather than at `s6`.

### What packing actually bought, against a corrected baseline

Aggregate throughput was 6,302 tok/s across the three arms, which is the sum of the per-arm
rates above. The nine-minute sizing trial called this a 1.51× card-hour gain by comparing
packed arms against 2,833 tok/s — but that solo figure came from the `C4` entropy-weighted
smoke over 30 steps, which is the most expensive arm in the sweep measured over its warmup.
Against the right baseline, `C1`'s own solo rate of 5,229 tok/s on an AWS L40S, the real
aggregate gain is **1.21×**, and the earlier 1.51× overstated it.

In card-hours: 9.198 for three arms is 3.07 per arm, against 3.48 solo on the same provider —
a 12% saving, not a 34% one. The gain that matters is elsewhere and it is not a throughput
gain at all. Three arms on one card left the second card free for the reference arm, so four
arms finished in one 9-hour window under a two-card cap that would otherwise have run them in
two rounds. Packing bought parallelism under the cap; the card-hour saving is a rounding
error beside it.

One design assumption was wrong and is worth correcting for Pack B's sizing. `C3` was placed
first on the reasoning that full-parameter tuning is the slowest arm and should start
earliest. It was not the bottleneck: it finished in 8.07 h against `C7`'s 9.17 h, and the
pack's wall clock was set by the raw-mix arm, which does 15% more steps because uniform
sampling draws shorter rows. Step count, not tuning method, predicted the tail here.

Memory came in well under its ceilings: 25.51 GB peak against a 42.9 GB headroom limit, with
`C3` holding 11.72 GB of the 22.0 GB it was given. The sizing trial's OOM at 11.0 GB was the
per-process ceiling doing its job, not the arm's true footprint — at 22.0 GB it ran clean, and
`pack_summary` reports a fourth arm would have fitted.

## The packed evaluation smoke failed on its own configuration, and the reason generalises (2026-08-26 07:33Z)

`18c83b6b` was queued at 07:05 alongside Pack B and was dead by 07:09:27, three minutes and
forty-six seconds in, before a single item was scored. The supervisor exited 2 on its own
validation:

```
pack_overrides is not valid JSON (Expecting property name enclosed in double quotes:
line 1 column 2 (char 1)): "{'E1': {'components': 'bfcl,probes'}, 'E2': {'components': 'ifstruct,ifeval'}}"
```

The value in that message is a Python `repr`, and the value I sent was JSON with double
quotes throughout. Reading the stored job back settles what happened:

```
job_data['pack_overrides'] -> type: dict
```

A `-p key=<value>` argument is **typed on the way in**. The platform parses a JSON-looking
parameter and stores the parsed object, which is the behaviour the parameter wanted in the
first place: `pack_overrides` is JSON specifically so `{"lora_r": 64}` arrives as an integer
and `{"smoke": false}` as a boolean. The supervisor then did `str(C("pack_overrides", ""))`
on the way out, and `str()` of a dict is a repr with single quotes, which `json.loads`
correctly refuses. Both halves were reasonable and the pair was not.

The fix accepts a mapping as-is, keeps `json.loads` for text, and falls back to
`ast.literal_eval` so a repr also parses:

```python
_ov_raw = C("pack_overrides", "")
if isinstance(_ov_raw, dict):
    OVERRIDES = dict(_ov_raw)
    _ov = ""
else:
    _ov = str(_ov_raw).strip()
```

**Why the local tests missed it, which is the part worth keeping.** `test_pack_schedule.py`
already covered `pack_overrides` in four ways: a patch that reaches the right child, a patch
that does not leak to the wrong child, and three malformed values that must be rejected. Every
one of them built the value with `json.dumps`, because a test that writes a config in Python
naturally writes text. The platform is the only caller that hands the supervisor a dict, so
the one code path in production was the one path with no coverage. The test now runs the same
patch three ways, as JSON text, as a dict, and as a repr, and asserts the child received its
patched value in each. Both contract tests and the staging build pass against the patched
supervisor.

The re-queue is `37b50115-2711-4a52-a243-795e3f9b18c4`, identical parameters, same L4. Cost of
the lesson: 0.063 GPU-h, billed for a cluster launch that did no work, and about twenty-five
minutes of wall clock on a path that is not the critical one. Pack B was unaffected, running
throughout, and does not use `pack_overrides`.

The patched supervisor has **not** been uploaded to the sweep pack task (`cfc6dec7`) while
Pack B is live on it; there is nothing to gain from touching the files under a running job,
and Pack B passes no overrides. It goes up when Pack B lands, before anything else queues
against that task.

## The packed scoring harness works, and looking for the weights it should score found them missing (2026-08-26 07:41Z)

`37b50115` completed in 2 m 35 s: 2/2 arms rc=0, zero assertion failures, 19 artifacts.

| arm | components | wall | numbers |
|-----|-----------|------|---------|
| E1 | bfcl, probes | 142.6 s | `bfcl_ast_composite` 0.6477, `probe_flag_rate` 0.0, `probe_false_flag` 0.0, `probe_stack_idiom` 0.200 |
| E2 | ifstruct, ifeval | 79.9 s | `ifstruct_validity` 0.125, `ifeval_prompt_strict` 0.750 |

Total wall clock was 151 s against 222 s of arm time, so the two arms genuinely overlapped
rather than queueing behind each other. Sixteen items a component on the published checkpoint:
the numbers are a smoke and are not comparable with the `s5.2` baseline rows. What is proven is
the mechanism the direction decision now depends on: per-arm config through `pack_overrides`,
two torch processes under separate ceilings on one L4, per-arm output and benchmark caches, and
`score.json` collected back per arm into a pack summary.

### The weights were never in shared storage, and four clean runs said nothing about it

With the harness working, the next step is to point it at each trained arm, which needs the
arms' weights on a storage path. There were none:

```
$ lab storage ls | grep s5.3
tidepool/s5.3/replay/replay.jsonl.gz
tidepool/s5.3/tooldata/clean_control.jsonl
tidepool/s5.3/tooldata/tooldata_train.jsonl.gz
```

No `tidepool/s5.3/arms/`, no `tidepool/s5.3/ckpt/`. The trainer does try, at the end of every
arm, and `C3`'s own console says what happened:

```
[C3] CLI storage upload failed ([Errno 2] No such file or directory: 'lab')
[C3] weights mirrored to tidepool/s5.3/arms/C3: False
```

**The reason is an assumption in the isolation design that was wrong.** A packed child is given
a stub in place of the job API, and the stub deliberately raises on storage calls so the caller
falls through to a CLI fallback. Its own docstring states the intent: *"storage goes through the
CLI and config arrives on the environment"*. The `lab` CLI is not installed on the machines
these jobs run on. So the fallback could never work, in any pack, ever, and it failed the same
way every time: one line in a per-arm console log that is only uploaded when the pack finishes,
a `False` in a message that reads like a status rather than a problem, and an exit code of zero.

What it cost: four trained adapters (`C1`, `C2p`, `C3`, `C7`) and every checkpoint of a
nine-hour pack. Nothing scientific was lost, because `lab.save_artifact()` runs from the
supervisor and every arm's `<arm>__adapter.zip` is intact on its job. But the checkpoint-resume
machinery built at 01:00Z, the whole point of which was that a stalled arm costs a quarter of an
hour instead of three, has been inert for every packed arm since it was written. It was never
exercised: `C1` re-ran from the top, and no arm has stalled since.

### The fix: the supervisor is the only uploader, as it is already the only reporter

The pack's isolation contract already says children never call `lab.*` because one job has one
progress stream and concurrent writers interleave into nonsense. Uploading is the same problem
with the same answer, and the previous design broke the rule by making children reach storage
on their own. Now a child writes a request and the supervisor performs it:

- the child appends one JSON object per line to `out/<arm>/mirror_pending.jsonl`, naming a file
  inside its own directory and the storage prefix it belongs under;
- the supervisor drains that file on every poll (5 s) with the job's own SDK, once more the
  moment a child exits, and once at the end. Draining *during* the run is what makes a mid-run
  checkpoint worth writing at all;
- a half-written final line is left for the next poll rather than treated as corrupt; a failed
  upload is retried on the next poll up to five times, then recorded as failed and moved past;
- `pack_summary.json` gains a `mirrored` block (per arm, per file, landed or not) and a
  `mirror_failures` list that is logged loudly. The trainer's `score.json` and `metrics.json`
  gain `weights_storage_dest` and `weights_mirror_requested`.

A mirror failure does not fail the arm. The job artifact is the guaranteed copy, and marking
nine hours of clean training as a failed arm would also stop whatever was waiting on it. What
must not happen is silence, and silence is exactly what happened four times.

Language changed with it. `_storage_put` returning True used to mean "in storage"; it now means
"on its way", and the log lines say which of the two this process can honestly claim
(`uploaded` for a solo run, `handed to the supervisor` for a packed child). The supervisor's log
is the only place that says it landed.

The four adapters were moved into `tidepool/s5.3/arms/<arm>/adapter.zip` by hand from their job
artifacts, so scoring is not blocked on the next pack: 40.1 MB each for the three LoRA arms,
1.7 GB for `C3`.

### A second bug on the same path, found by looking at what `C3` actually wrote

`C3` is the full-parameter arm, and its archive is not an adapter:

```
C1/adapter.zip : adapter_config.json, adapter_model.safetensors (44 MB), tokenizer*, README
C3/adapter.zip : config.json, model.safetensors (2.34 GB), generation_config.json, tokenizer*
```

Tuning every weight leaves nothing to adapt, so `save_pretrained` wrote a whole model. The eval
harness only knew the adapter shape, and `adapters.resolve()` would have raised on `C3` -- which
is the right failure (its docstring is explicit that a harness catching the load error and
carrying on would report a full set of plausible scores for the base model under an arm's name),
but it means the full-parameter arm could not be scored at all. It now classifies the directory
by looking at it: `adapter_config.json` means merge onto the base, `config.json` plus a
`model*.safetensors` means load it *as* the model and merge nothing. Which one happened is
recorded in the run summary as `weights_kind`, because an arm mislabelled either way loads
without complaint and scores the wrong weights.

The harness also now reports `peak_gpu_gb`, `peak_gpu_reserved_gb` and `card_total_gb`. The
first eval pack was sized by guessing 10 GB an arm on a 24 GB card, which fits two and would
have refused a third with no evidence either way; the sweep learned the same lesson in the other
direction and sizes packs on reserved memory plus a context allowance.

### What is running

`97939c69` is a four-arm smoke, `C1`/`C2p`/`C3`/`C7` on one L4 at 16 items a component under
5.2 GB ceilings, `run_tag` `s5.3-arms-smoke`. It is queued for two answers rather than any
score: that the full-parameter path loads a real checkpoint, and what an arm actually holds, so
the full scoring pass is sized on a measurement. Its numbers are not comparable with the `s5.2`
baseline rows and must not be quoted beside them. Pack B (`1df3bf2b`) is training throughout at
7%.

Both contract tests were extended before any of this shipped: `test_pack_schedule.py` now
covers mirroring on request (two arms, three files, the exact object keys asserted, the summary
block checked, and a request naming a missing file that must not take the pack down), and
`pack_overrides` arriving as JSON text, as a dict and as a Python repr. It also caught a
real bug in the handover path while doing it: `release()` was passing a whole object path where
the SDK wants a destination *prefix*, which would have written
`.../replay.jsonl.gz/replay.jsonl.gz`. The test double now models prefix semantics exactly, so
that class of mistake fails locally instead of in a nine-hour job.

The patched supervisor is on the eval task. It is **not** on the sweep task (`cfc6dec7`) while
Pack B is live on it, so Pack B keeps the old behaviour: its checkpoints will not reach storage
and its four adapters will need the same manual move. That is a known, bounded cost, and it goes
up before anything else queues against that task.

## The four-arm scoring smoke failed on all four arms and paid for itself (2026-08-26 07:52Z)

`97939c69` ran 3m46s of arm time on an L4 and exited with `0/4 arms completed (none);
FAILED: C1 rc=1, C2p rc=1, C3 rc=1, C7 rc=1`. It was queued for two answers and neither
of them was a score. Both came back, and the failure is a third answer worth more than
either.

### The full-parameter checkpoint loads

`C3` tunes every one of the 1.2B weights, so its archive holds a `config.json` and a
2.34 GB `model.safetensors` and no `adapter_config.json`. Line 3 of its log:

```
[C3] tidepool/s5.3/arms/C3/adapter.zip is a full fine-tuned checkpoint, so it is loaded
     as the model itself and nothing is merged onto it
```

It then loaded and reached generation. The `adapters.kind()` classification and the
branch in the hf path both work against a real checkpoint, which is the one thing about
them that had never been true before this run.

### The adapters are readable from storage

All four arms resolved:

```
[C1] adapter for tidepool/s5.3/arms/C1/adapter.zip resolved to out/C1/weights
[C1] merged LoRA adapter from out/C1/weights
[C1] loaded LiquidAI/LFM2.5-1.2B-Instruct (1170340608 params, bf16, padding_side=left)
```

So the hand repair of the four Pack A adapters landed them where the harness looks for
them, and the native chat template was accepted with tool turns (`template mode: native
… tools= argument supported: True`) exactly as it was for the baseline rows.

### The ceiling, and not the card, killed every arm

Each of the four died at the first `generate()` call in BFCL, in prefill, inside the
FFN:

```
torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 772.00 MiB. GPU 0 has a
total capacity of 22.04 GiB of which 17.33 GiB is free. … this process has 4.70 GiB
memory in use. 4.78 GiB allowed
```

That quote is `C7`, the last arm alive. **17.33 GiB of the card was free when it was
killed.** `torch.cuda.set_per_process_memory_fraction` enforces the per-arm cap whatever
the card has spare, so the isolation did exactly what it is built to do and the number I
gave it was wrong. Every arm shows the same shape: 4.41 GiB allocated by the allocator,
772 MiB wanted, 4.78 GiB allowed.

The 772 MiB is one FFN activation: 404M bf16 elements over an 8192-wide intermediate is
about 49,400 tokens, or batch 16 at roughly 3.1k tokens a prompt. BFCL's live categories
carry a dozen tool schemas in the prompt and run that long. Prefill peak is therefore
batch times sequence length, and it is reached on the *first* batch of the *first*
component, which is why the whole job was over in under four minutes. **An undersized
ceiling in this harness fails fast and cheap**, and that matters for how the real pass
was sized below.

### Scoring is a different appetite from training

The sweep's measured training figures are 7.5 GB for a LoRA r16 arm, 11.4 for the
entropy-weighted arm and about 22 for the full-parameter arm. Those are gradients and
optimizer moments, and none of them exist at inference: a merged r16 adapter and a
full-parameter checkpoint are both a 1.2B bf16 model, 2.34 GB of weights. What scoring
holds instead is the prefill activation and the KV cache, and at batch 16 on BFCL that
is a little over 5.2 GB plus about 0.3 GB of CUDA context per process.

Two consequences, both recorded in `pack.yaml` next to `pack_gb` so the next reader does
not repeat the guess:

- **Two arms an L4, at 11 GB each.** Four does not fit at this batch size, and 11 GB is
  roughly twice the measured need, which the splitting below makes sufficient.
- **Packing buys much less here than in training.** Training leaves the card idle
  between steps, which is what three arms sharing one recovered as 1.51x aggregate
  throughput. Generation saturates it, so there is little idle capacity to reclaim by
  crowding. Two arms over four jobs and four arms over two jobs cost about the same
  card-hours, so the sizing question and the throughput question have the same answer.

### The serving loop now degrades instead of dying

Raising a ceiling is a guess that has to be right. `gen.py` now halves a group that does
not fit and retries it, down to a single prompt, which is a guess that does not have to
be:

```python
        except _OOM:
            self.oom_seconds += time.time() - t0
            del enc
            try:
                torch.cuda.empty_cache()
            except Exception:
                pass
            if len(idx) == 1:
                raise
            self.oom_splits += 1
            half = len(idx) // 2
```

Four points on the shape of it:

- **Only a single prompt that cannot run is still fatal.** Everything above that is a
  slowdown, isolated to the arm that hit it.
- **Time spent on an attempt that produced nothing is banked in `oom_seconds`** and kept
  out of the tokens-per-second denominator, so a split does not flatter throughput.
- **`oom_splits`, `oom_seconds` and `smallest_batch` reach `score.json`.** A ceiling set
  too low is visible in the result rather than inferred from a wall-clock that looks off.
- **The exception name is resolved at import**, from `torch.OutOfMemoryError` or
  `torch.cuda.OutOfMemoryError`, so the harness is not pinned to one wheel's spelling.

The premise the split rests on is the one already in the module docstring: greedy argmax
with left padding does not depend on how the prompts were grouped. What the split does
break is the old claim that batch composition is a function of the item order alone, and
the docstring now says so, because composition depends on memory pressure from sibling
arms.

`test_gen_oom_split.py` covers it without a GPU by stubbing `torch` and standing a fake
model in that refuses any batch above a set size: 16 checks over a ceiling nothing trips,
a ceiling that admits 4 (halving sequence `16,8,4,4,8,4,4`, order preserved, cache
dropped once per failed attempt), an odd group of 5 (`5,2,1,1,3,1,2,1,1`), a single
prompt that cannot run at all, and the counters reaching `throughput()`.

### What is running

- `385e210a` scores **C1 and C3** on all four components at **full item counts**, two
  arms on one L4 under 11 GB ceilings, `run_tag` `s5.3-arms`. Card, batch size, item
  counts and component list are the B1-B6 baseline settings unchanged. Three of these
  arms sit within 0.0006 of each other on validation loss, so anything that could move a
  number by that much is held still, and that is why the L4 was not traded for a bigger
  card: the header of `pack.yaml` already fixes the card as non-negotiable because every
  full-precision number in this project was taken on one.
- `1df3bf2b` (Pack B: C4, C5a, C5b, C6, RB) is training at 14%.

C2p and C7 wait for a GPU slot; the account cap is two and both are held. Pack B's four
adapters will need the same hand move into `tidepool/s5.3/arms/` that Pack A's did,
because the patched supervisor is deliberately not on the sweep task while Pack B is live
on it.

Spend 28.71 of 145 GPU-hours.

## The decision machinery, built while the cards were busy (2026-08-26 08:12Z)

### Scoring costs a fifth of what it was priced at

Pass 1 has produced its first hard number about itself. C1 and C3 crossed 1,936 and 1,776
of 3,490 items about fourteen minutes into generation, which is roughly 130 items a minute
an arm, and `grep 'out of memory'` over the whole log so far returns nothing: the 11 GB
ceiling holds at batch 16 and `gen.py`'s halving path has not fired once.

The s5.2 screening figures priced a full-count pair at about 3.5 card-hours. At the
measured rate a pair finishes generation in well under an hour, and even with the CPU-bound
grading pass on top the pair lands nearer one card-hour than three and a half. So all eight
arms at full item counts cost a few card-hours against 116 unspent, and the reduced
screening profile the s5.2 plan called for is not needed at all. The departure recorded in
the last report entry stands, and it turns out to have been cheaper than the plan it
departed from rather than more expensive.

The pass-2 script is written and holds that reasoning, along with an instruction to re-check
the completed log for split retries before running: a ceiling that only just held through
half an item set is not a ceiling that held.

### The sweep cannot pick a winner, so something else has to

The selection metric this sweep was designed around has failed at its one job. Three of the
four finished arms sit within 0.0006 of each other on validation loss at one seed apiece,
which is not a ranking. s5.4 therefore has to be decided on the task metrics, and reading
those as a table of rates would repeat the same mistake at a larger scale: eight arms over
several components produce dozens of small differences, and among differences that small a
ranking is as likely to be sampling noise as signal.

`jobs/s5-compare/` is the correction for that, registered as task
`ecd12a36-da98-4a18-bc38-2889905746e9`. It is CPU-only and draws nothing from the GPU
allowance, because the graders already wrote a verdict per item and a bootstrap over three
counts is arithmetic. It is a job rather than a local script for provenance: a bootstrap
interval is a reported statistic, so it carries a job id like every other number here.

Three properties are worth naming.

**It joins arms item by item.** Item difficulty drops out of the comparison, which is the
only reason a two-point gap over a few thousand shared items can be resolved at all. The
per-item verdicts live in `scored_*.jsonl`; the sibling `completions_*.jsonl` holds the raw
generations and carries no verdict, so an earlier plan to read the completions files would
have produced an empty comparison.

**Every cell carries an interval and an exact test, and the family is corrected.** A
percentile bootstrap over the three paired-difference counts, McNemar's exact two-sided test
on the discordant pairs, and Holm-Bonferroni across every arm-by-component cell in one run.
Twenty-eight cells against one reference will produce a nominally significant result by
chance about once even when every arm is identical, so the table's verdict column has three
states and one of them is `indistinguishable`. `queue_compare.sh` refuses the obvious way to
break this: all arms go in one call, because splitting them over two jobs would correct each
half against a family half the size and report p-values that are too small in both.

**The probe file is split into its four populations rather than averaged.** An arm can raise
its detection rate purely by flagging more often, and pooled with the clean control arms that
trade vanishes into a single number. Detection, the synthetic clean arm, the corpus clean arm
and the stack-idiom probes are four separate cells.

One honest limit is stated in the module docstring and in the rendered table. The paired
delta is item-weighted, while BFCL's headline in `score.json` is a macro average over eleven
categories, which weights a 40-item category like a 400-item one and therefore has no
per-item pairing to test. Where a component names a grouping field the macro-averaged paired
difference is reported beside the item-weighted one, as a description with no interval and no
test attached.

`test_stats.py` (31 checks) pins McNemar against hand-computed binomial tails, the interval
half-width against its own analytic standard error, sign symmetry, seed reproducibility, and
a Holm worked example. `test_main.py` (32 checks) runs the driver end to end over fixtures
with the SDK stubbed, and the fixtures are built so the answer is known in advance. The BFCL
fixture is the one that earns its keep: ten items in two categories where the item-weighted
delta is +0.1 and the macro delta is -0.125, so a run reporting only one of the two columns
would report the opposite direction. The rest cover the row filters selecting the right
population, a missing file being dropped and named rather than compared, a repeated item id
being reported rather than silently shrinking the denominator, and a reference outside the
arm list being refused rather than guessed at.

### Getting the verdicts to the comparison

A packed arm writes locally and the supervisor uploads its files as job artifacts, flattened
under an `<arm>__` prefix. The comparison reads storage objects under
`tidepool/s5.3/arms/<arm>/`, because a comparison spans several jobs and cannot be pinned to
any one of them. `promote_scores.py` moves them across and strips the prefix. It computes
nothing, and it refuses to overwrite an existing object unless forced, because a rescore
landing on top of the bytes a recorded comparison was drawn from is the one way that step
could quietly invalidate a result. Dry-run verified against the finished `37b50115`.

## The reliability gate had nothing wide enough to test against (2026-08-26 08:34Z)

Writing the s5.4 decision rule before the numbers turned up a gap that reading the numbers
afterwards would have hidden. The rule's first gate is the project's own reliability
criterion, detection at or above 0.70 with a false-flag rate at or below 0.15, and I wrote it
with the 138-item corpus clean arm governing over the 30-item synthetic one, because that is
what the criterion was revised to be measured against at s5.3.

Checking the queued configuration rather than assuming it: `clean_control_object` is empty in
`pack.yaml`, and none of the arm queue scripts sets it. **No arm in the sweep is scored
against the corpus clean arm at all.** The default is empty deliberately, so a re-run of an
already-published baseline row stays byte-comparable to the row it replaces, and the arm
scripts inherited it without anyone deciding to.

The fix is not to set it for the remaining passes. C1 is the reference every other arm is
paired against, and C1 was scored without the arm, so a corpus cell on arms 2 through 8 would
have nothing to pair against. All eight arms stay identical.

So gate 1 is evaluated at s5.4 on the thirty synthetic items, and what that costs is worth
being precise about. Thirty items bound a rate only to about one item in thirty, so the arms
cannot establish the absolute 0.15 ceiling. The comparison s5.4 actually needs survives
anyway: every arm sees the same thirty items, paired, so an arm that over-flags is visible
against C1 whether or not thirty items can price the rate. An arm flagging a fifth of clean
returns puts six discordant pairs against C1's zero, which McNemar resolves without
difficulty. The absolute ceiling is a claim about the delivered model, and it is verified at
s6.

`queue_arms_probes.sh` closes the gap for the whole sweep rather than for the winner alone:
probes only, `clean_control_object` set, four arms a card at 5.5 GB, 138 extra items an arm
and a few card-minutes for all eight. It runs after the four scoring passes and before s5.5,
and the rescope trigger in the decision rule is read against it rather than against the
thirty. Four arms fit where two did because probes carry none of BFCL's 3.1k-token tool
schemas, and 5.5 GB sits deliberately near the measured 5.2 GB floor now that `gen.py` halves
a group it cannot fit: a ceiling set too low costs throughput and no longer costs the arm,
and `oom_splits` and `smallest_batch` in each `score.json` report whether it was.

Its output collides with the full pass's `scored_probes.jsonl` and should: the same graded and
synthetic-clean items plus the corpus arm, on the same model under greedy decoding, so the
wider file supersedes the narrower one. Only that file is promoted, with `--force`. The full
pass's `score.json` is left alone, because a probes-only `score.json` carries no tool-calling,
structured-output or instruction-following rates and overwriting it would destroy what the
comparison cross-checks its recomputed rates against. The shared items come with a free
determinism check attached: greedy argmax with left padding does not depend on how prompts
were grouped, so a verdict that moved between the two passes on a shared probe item is a real
finding rather than a rounding difference.

## What is queued, ready, and waiting (2026-08-26 08:45Z)

`385e210a` is at 78% with both arms level at 2,736 of 3,490 items and still no out-of-memory
split anywhere in its log. The back half of the item set is slower per item than the front,
which is expected: IFStruct and IFEval allow 768 new tokens against BFCL's 320. The ledger's
estimate for the job is revised from 3.0 card-hours to 1.2 on its own measurement rather than
on the s5.2 screening figures, and the actual will replace it when the job lands.

Ready to run, in order, nothing waiting on authorship:

1. `queue_arms_pass2.sh` — C2p + C7, the moment a card frees.
2. `queue_arms_pair.sh C4,C6` and `queue_arms_pair.sh C5a,C5b` — passes 3 and 4, once Pack B
   lands and its adapters are moved. The pair script exists so the remaining pairs cannot
   drift from passes 1 and 2 by retyping; a settings difference between pairs would be larger
   than the effect the pairs exist to measure. It refuses anything other than two arms, since
   11 GB apiece is what an L4 holds.
3. `queue_arms_probes.sh` — twice, four arms a card, closing the corpus clean-arm gap across
   the whole sweep.
4. `promote_scores.py <job> --arms ...` after each scoring pass, and
   `--include adapter.zip` after Pack B, which turns Pack A's hand move into a repeatable
   step with an overwrite guard on it.
5. `queue_compare.sh C1,C2p,C3,C4,C5a,C5b,C6,C7` — once, with every arm in the one call.

Still outstanding from earlier runs, both needing a card: the supplementary clean-arm re-score
over baselines B1 through B4, and resume-from-storage inside a pack, which remains
unimplemented. Neither blocks s5.4.

## The replay handover worked, and the buffer is sound (2026-08-26 08:52Z)

The open question from the Pack B launch was whether the buffer arm's handover would leave
the good file on local disk while shared storage kept the broken one. It did not. RB exited
rc=0 after 49.1 minutes, the supervisor registered its output as the stand-in for
`tidepool/s5.3/replay/replay.jsonl.gz`, uploaded it, and logged `placed ... in shared
storage`. C5a and C5b started immediately behind it and are both stepping.

The buffer itself was pulled down and read: 7,945 rows, `n_tok` from 23 to 1,332 with a
median of 388, 3.21M tokens indexed in total. The earlier buffer indexed the same 7,945 rows
at `n_tok` 2, which is a generator counting its own output wrong; the sanity guard in the
trainer refuses to dose from a set like that, and the fact that C5a and C5b are training at
all is that guard passing. Both numbers now agree with the file: a 1% dose draws about 640k
tokens from a 3.2M-token set, so each replay row is seen roughly a fifth of a time, and no row
is replayed at all in the memorization sense.

Worth reading the step denominators correctly, because they look wrong at a glance. C4 and C6
plan 8,416 steps, C5a 8,429 and C5b 8,481: the replay arms are only 13 and 65 steps longer,
not 1% and 5%. That is the design. The token budget is fixed at 64.0M for every arm, so replay
displaces base data rather than adding to it, and the small difference is packing granularity
on shorter rows. The 13-to-65 ratio being exactly one to five is the check that the dose is
what it says it is.

## The progress bar was one component, not the run (2026-08-26 09:10Z)

I read `385e210a`'s progress wrong, and the cost estimate built on that reading was wrong with
it. Correcting both here, and correcting the ledger, the task list and the stage headline.

What happened. The supervisor's progress line reports each child's last `step i/n` or
`generated i/n` line, and the eval harness calls `generate()` once per component with that
component's own item count as the denominator. `3490` is BFCL at full item counts, not the
run: eleven categories summing to 3,514 published items, 3,490 after decontamination. So
`progress 97% | C1 3376/3490` did not mean the pass was nearly done. It meant BFCL was nearly
done at the first of two calling conventions, with the second convention, 2,000 structured-
output items, 541 instruction-following items and 434 probes still ahead.

The tell arrived a minute later and I nearly misread it a second time: `progress 49% | C1
16/3490 C3 3376/3490`, C1 apparently thrown back to the start. There is no supervisor line
about a child exiting anywhere in the log, and `pack.py` prints one on every child exit, as
Pack B's `RB exited rc=0` shows. Nothing restarted. C1 simply moved to the second BFCL
convention and began a fresh count under the same denominator, one poll ahead of C3.

Two lessons worth keeping, since both were available before the numbers and I took neither.
A progress denominator that stays constant across a phase change is indistinguishable from a
restart, and the way to tell them apart is the exit line, not the counter. And a rate measured
inside one component does not extrapolate: `generate()` sorts its prompts by length and runs
the short ones first, so the front of any component clears at three or four times the average
and the figure I called "roughly 130 items a minute" was the easy end of one component.

### What it actually costs

The measurement that should have anchored this was already in the ledger. `4350ce4e`, B1's
full pass, ran the same four components at the same full item counts across both calling
conventions, solo on an L4, in 85 minutes and 1.42 card-hours. Two arms packed on the same
card do twice the generation. Where that lands depends on how much of the card one arm was
using, which the pack was built to find out:

- On generation count, `385e210a` is about 42% through after 35 minutes of child time, which
  extrapolates to roughly 1.4 hours for the pair and implies packing is nearly free here.
- On B1's solo cost doubled, the pair is 2.8 hours and implies packing buys nothing.

The truth is between them, and the remaining components push toward the upper end: IFStruct
and IFEval allow 768 new tokens against BFCL's 320, so their items are slower than the ones
the 42% was measured on. The ledger estimate is set to 2.0 hours and the actual will replace
it. Four passes is then about 8 card-hours rather than the 4 I claimed at 08:12Z, against 116
unspent.

### The decision it was used to justify still holds, for a different reason

At 08:12Z I withdrew the screening-profile fallback because full-count scoring looked like
about a fifth of its estimated price. That reasoning is void. The conclusion is not, and the
reason is worth stating plainly rather than quietly keeping: 8 card-hours of the 116 that are
unspent buys the paired test its full item count on every component, and the s5.4 rule needs
that count because the effects it is looking for are small enough that three of four arms
could not be separated on validation loss at all. Screening would have saved about 6 card-hours
and cost the comparison most of its power. Full counts stay, priced honestly.

The ceiling result is unaffected and still worth having: 11 GB an arm, batch 16, zero
out-of-memory splits across 4,626 generations an arm so far, and `gen.py`'s halving path has
not fired once.

### Whether the corrected price changes the schedule (2026-08-26 09:15Z)

Four scoring passes at about two hours each is eight hours of wall clock on one slot, and the
account cap is two with Pack B holding the other. Worth asking whether the passes can be made
wider, since the critical path to s5.4 is now scoring rather than training. Three options, none
of them taken:

**More arms on the L4.** Four at 5.2 GB was measured and every arm died in prefill; two at
11 GB holds. Three at 7.3 GB was never tried and would fit, since an arm needs a little over
5.2 GB of allocator space plus about 0.3 GB of context at batch 16. It buys nothing. Generation
saturates the card, which is why `pack.yaml` says packing helps far less here than in training,
so three arms a card runs about half again as long per pass: three passes of three hours against
four of two. The arithmetic is a wash and the wider pack is the riskier of the two.

**A bigger card.** Four arms at 11 GB fit comfortably on the L40S Pack B is using, which would
halve the passes. Refused, and the reason is in `pack.yaml`: every full-precision number in
this project was taken on an L4. C1 is already scored on an L4 in this very pass, so moving the
remaining arms would put the reference and the arms it is paired against on two sets of
numerics, and a paired test across that boundary measures the card as much as the recipe.
Re-scoring C1 on the L40S would fix the pairing and break the comparison to the B1 through B6
baseline rows that s6 needs.

**Reordering.** Not available. Pass 2 (C2p, C7) can go as soon as a slot frees, because Pack A
already put those adapters in storage. Passes 3 and 4 read Pack B's checkpoints, which do not
exist yet, so the order is forced by the dependency and not by a choice.

So the schedule stands: pass 2 into the freed slot, passes 3 and 4 behind Pack B, the probes
pass over all eight arms after them, and the comparison last. Pack B is about a quarter through
its four remaining arms, which puts the two tracks finishing within a few hours of each other.

## Pass 1 landed; the "zero OOM splits" claim was wrong (2026-08-26 10:30Z)

`385e210a` (C1, C3) finished clean at 2.473 GPU-h, close to the 2.0 h estimate: 2/2 arms rc=0,
30 artifacts, no assertion failures, both arms scored at full item counts on all four
components. Both arms' scored files and score/eval-summary JSON are promoted into
`tidepool/s5.3/arms/C1/` and `tidepool/s5.3/arms/C3/` for the compare job (dry run showed no
clashes; the real move copied 14 files).

Scores:

| arm | bfcl_ast_composite | ifstruct_validity | ifeval_prompt_strict | probe_flag_rate | probe_false_flag |
|---|---|---|---|---|---|
| C1 | 0.7196 | 0.1375 | 0.7523 | 0.6111 | 0.0 |
| C3 | 0.7110 | 0.1450 | 0.7837 | 0.6037 | 0.0 |

A correction to an earlier claim in this file and in `tasks.md`: I wrote that the pair cleared
"ZERO out-of-memory splits" through 4,626 generations. That was read off the live progress line,
not the finished console logs, and it is wrong. Each arm hit exactly one OOM split, both in
`bfcl/tools_text` near the tail of the item set (around item 3376-3490 of 3490): `out of memory
on 16 prompts, retrying as 8 + 8`. Both retries succeeded, no completions were lost, no
assertion failed, and both arms still finished every component at full item count. `gen.py`'s
halving path did fire, contrary to what I recorded -- it just worked, silently, exactly as
designed, and I hadn't grepped the console for it before writing the summary line.

This does not change the decision to run pass 2 at full item counts, and it does not change
the ceiling: 11 GB an arm at batch 16 still holds, with one graceful degrade per arm rather than
zero. It does mean `score.json` carries no `oom_splits` or `smallest_batch` field despite the
plan (`test_gen_oom_split.py`'s docstring) saying the counters would reach it -- that gap is
real and is worth closing before `s6` leans on this harness, but it is not a blocker: the
console log is the source of truth in the meantime and I've now read it for both passes.

Pass 2 (C2p, C7) is queued as `8b7bad6f-4d26-42d3-b0ed-7a19c7e8cd52`, same settings, into the
slot pass 1 freed. Both pass-2 arms are rank-16 adapters (lighter than pass 1's full-parameter
C3), so the OOM finding, if it recurs, would point at the `bfcl/tools_text` component itself
rather than at arm size -- worth checking pass 2's console for the same line either way.

Pack B (`1df3bf2b`) is healthy and unaffected: 37% at this check-in (C4 2100/8416, C6
2100/8416, C5a 1470/8429, C5b 1484/8481), steadily climbing since the last check-in, no stall.

Spend 31.183 of 145.

## Pass 2 lands: C7 is the first arm to clear all three s5.4 gates (2026-08-26 13:15Z)

`8b7bad6f` (C2p, C7) finished clean at 2.541 GPU-h against a 2.0h estimate: 2/2 arms rc=0, 30
artifacts, 0 assertion failures, both arms scored at full item counts on all four components.
Scored files promoted to `tidepool/s5.3/arms/C2p/` and `tidepool/s5.3/arms/C7/` for the compare
job (dry run showed no clashes; 14 files moved).

Scores:

| arm | bfcl_ast_composite | ifstruct_validity | ifeval_prompt_strict | probe_flag_rate | probe_false_flag |
|---|---|---|---|---|---|
| C1 (ref) | 0.7196 | 0.1375 | 0.7523 | 0.6111 | 0.0 |
| C2p | 0.7070 | 0.1390 | 0.7283 | 0.0074 | 0.0 |
| C7 | 0.7049 | 0.1400 | 0.7338 | 0.7222 | 0.0 |

Reading this against the decision rule frozen at 08:20Z (`tasks.md` s5.4, "the s5.4 decision
rule, written before the numbers"), evaluated on the 30-item synthetic clean arm since
`clean_control_object` is still unset for every arm:

- **Gate 1** (detection >= 0.70, false-flag <= 0.15): C7 is the first and only sweep arm to pass
  — 0.7222 detection, 0.0 false-flag. C1 (0.6111) and C3 (0.6037) both sit under the floor; C2p
  (0.0074) barely flags at all, which reads as the guardrail block genuinely doing nothing when
  removed from training, exactly as C2p was designed to test.
- **Gate 2** (no BFCL category > 3.0 below C1, IFEval no more than 2.0 below): C7 is 1.47 points
  under C1 on BFCL and 1.85 points under on IFEval — inside both ceilings.
- **Gate 3** (IFStruct >= C1's 0.1375): C7 at 0.1400 clears it.

One arm outstanding (C6, training in Pack B) still shares C7's raw-mixture recipe; C4, C5a, C5b
remain. Nothing here is a decision yet — gate 1 is being read against the 30-item synthetic arm
under the amendment recorded at 08:34Z, not the 138-item corpus arm the plan originally
specified, and the probes-only pass with `clean_control_object` set is still queued behind these
four. But this is the first result since the sweep design was frozen that separates an arm from
the pack on the axis the whole substage exists to measure, and it lands on the recipe already
flagged as the sweep's "one clean effect" back when only val_loss was in.

Pack B (`1df3bf2b`) is healthy and unaffected: 54% at this check-in (C4 3780/8416, C6 3780/8416,
C5a 3360/8429, C5b 3392/8481), steadily climbing since the last check-in, no stall.

Spend 33.724 of 145.

## Pack B lands: all eight arms are trained (2026-08-26 20:40Z)

`1df3bf2b` finished at 20:34:28Z after 13h29m, 5/5 arms rc=0 (the RB replay buffer plus C4,
C6, C5a, C5b), 28 artifacts uploaded, 0 assertion failures anywhere in the pack. Billed
13.411 GPU-hours on one L40S. Every arm hit its token budget: 65.57-65.58M against the
64.0M target, so the sweep's equal-cost assertion holds on all eight arms.

Aggregate throughput 5,652 tok/s across the pack, 1,361-1,454 tok/s per arm. Peak concurrent
demand 41.5 GB of the card's 47.7, pack peak footprint 36.32 GB, 6.61 GB spare under the
headroom, and the supervisor's own arithmetic says a sixth arm would not have fit. Four
training arms on one L40S is therefore the ceiling for this recipe, not a conservative choice.

**Validation loss, all eight arms, at the identical 64.0M-token budget:**

| Arm | Delta from C1 | val_loss | vs C1 |
|---|---|---:|---:|
| C2p | guardrail_epochs 0 | 0.1511 | -0.0006 |
| C3 | full-parameter, LR 1e-5 | 0.1513 | -0.0004 |
| C4 | entropy-weighted loss | 0.1515 | -0.0002 |
| **C1** | **reference** | **0.1517** | **---** |
| C5a | 1% replay | 0.1517 | 0.0000 |
| C5b | 5% replay | 0.1526 | +0.0009 |
| C6 | LoRA r64 a128 | 0.1539 | +0.0022 |
| C7 | raw mixture | 0.1565 | +0.0048 |

Seven of eight sit inside 0.0028 of each other, and the reference is fourth of eight. The
spread across the whole sweep is 0.0054, which is smaller than the gap between the two
guardrail settings would need to be for validation loss to distinguish them at all. That is
the reason the `s5.4` rule was pre-registered on task metrics rather than on this column, and
nothing in Pack B changes it. Worth stating plainly: **C7 is last on validation loss and is
so far the only arm to clear all three s5.4 gates.** An arm chosen on this column would be
the wrong arm.

### The OOM check, done properly this time

Pass 1's summary claimed zero out-of-memory splits off the live progress line and was wrong.
So this pack's console logs were pulled and grepped before anything was written here: all
five logs, for `out of memory`, `retrying as` and any traceback or assertion. **No matches in
any arm.** Training genuinely did not degrade, which is the expected result --- the halving
path that fired in pass 1 lives in `gen.py` and is a generation-time behaviour, and Pack B
does no generation outside RB. The logs are otherwise dominated by the W&B SDK's own
telemetry-export errors (`failed to upload metrics ... too many concurrent telemetry
requests`), which are the sidecar reporting on itself and not the run: every arm's metrics
reached the dashboard.

Each arm has its own W&B run under `james-navinhill/tidepool` --- C4 `1hw9zt6h`, C6
`pl42vxd9`, C5a `bgejizda`, C5b `rvg4nk5o` --- so the four curves are separable rather than
overlaid.

### Adapters moved, and one of them is not the size of the others

`promote_scores.py 1df3bf2b --arms C4,C6,C5a,C5b --include adapter.zip` moved all four into
`tidepool/s5.3/arms/<arm>/adapter.zip`. Dry run showed no clashes, so no `--force` and nothing
superseded. This is the repeatable version of the hand move Pack A's four adapters needed.

C4, C5a and C5b are 42.1 MB apiece, matching C1 and C2p to within a kilobyte. **C6 is 165.5
MB**, very close to 4x, which is exactly what rank 64 against rank 16 should weigh and is a
cheap independent confirmation that `-p arm=C6` applied its delta rather than silently
training the reference recipe a fifth time. The same check passed on Pack A via C3's 1.85 GB
full-parameter archive.

### Passes 3 and 4 are queued, both cards busy

Both free slots went straight out, since the account cap is two and nothing else is holding
one:

- `ade4c590-2e91-490d-8131-953015803a0c` --- C4 + C6
- `acc01fdf-394c-4a59-ad12-04a778e161bb` --- C5a + C5b

Both via `queue_arms_pair.sh`, so the settings are byte-identical to passes 1 and 2 and to the
B1-B6 baseline rows: L4, 11 GB per arm, all four components at full item counts, both calling
conventions. Passes 1 and 2 cost 2.473 and 2.541 GPU-hours, so these two together should land
near 5 and finish within a few minutes of each other. C6 is the one unknown: a rank-64 adapter
merges to the same 1.2B model as a rank-16 one, so its generation cost should be identical,
and if it is not, that is a finding rather than a problem.

After these land: the probes pass over all eight arms with the corpus clean arm set (two jobs,
four arms a card), then the comparison job over all eight, then `s5.4`.

Spend 47.135 of 145.

### A second scoreboard existed for pass 2, and it was wrong (2026-08-26 20:50Z)

Cross-checking the pass-2 numbers before building on them turned up two records of the same
job that disagree on every field. The table in this file, written at 13:15Z, says C2p scored
0.7070 / 0.1390 / 0.7283 with a probe flag rate of 0.0074 and no false alarms. The status note
recorded against the same job says 0.7104 / 0.1391 / 0.7488, flag rate 0.3889, false-flag
0.0333. The C7 row differs the same way (0.7038 / 0.1458 / 0.7411 against 0.7049 / 0.1400 /
0.7338).

`score.json` as promoted into `tidepool/s5.3/arms/<arm>/` settles it, and it is the file the
comparison job reads:

```
C2p  bfcl 0.707   ifstruct 0.139  ifeval 0.7283  flag 0.007407  false 0.0
C7   bfcl 0.7049  ifstruct 0.14   ifeval 0.7338  flag 0.722222  false 0.0
```

The table in this file is right to four decimal places on every field. The status note's
figures match nothing on disk, in either arm, and are withdrawn; the note has been rewritten
against the files with the correction stated in it. Nothing downstream was computed from the
withdrawn numbers, because nothing downstream has run yet.

Two things are worth taking from it. The gate reading at 13:15Z stands unchanged, since it was
taken from the table. And the finding it reads gets *stronger*: C2p's flag rate is 0.74%, not
the 38.9% the note claimed, and 0.0074 is precisely the rate the untrained B1 reference posts.
Removing the guardrail block from training does not weaken the flagging behaviour by a third,
it returns the model to never flagging at all. That is the cleanest possible read of what the
C2p cell was built to test, and it was almost lost to a transcription.

The operator-facing headline had been written from the note rather than the table, so it had
been telling them 39% since 13:15Z. It is corrected, with the correction shown rather than
quietly swapped.

**The practice that caught it is worth keeping**: read the promoted `score.json`, not any prose
summary of it, whenever a number is about to be built on. Both the pass-1 out-of-memory claim
and this one came from writing down a number from somewhere other than the file that holds it.
The comparison job already works this way by construction, which is the reason it exists.

### Pass 4 lands: C5a and C5b, neither clears gate 1 (2026-08-26 23:12Z)

`acc01fdf-394c-4a59-ad12-04a778e161bb` (C5a + C5b, same L4/11GB setup as passes 1-3) finished
at 23:05:40Z, 2.355 GPU-h, 2/2 arms `rc=0`, all 30 artifacts verified present. Scores read off
the promoted `score.json` in `tidepool/s5.3/arms/{C5a,C5b}/` — checked against the job record's
own `score` dict this time before writing anything down, and the two agree on every field, so
no correction to make here:

```
C5a  bfcl 0.7013  ifstruct 0.1515  ifeval 0.7837  flag 0.5741  false 0.10
C5b  bfcl 0.7227  ifstruct 0.1095  ifeval 0.7708  flag 0.6333  false 0.00
```

Read against the three pre-registered gates (baseline C1, 30-item synthetic clean arm per the
08:34Z amendment): **neither arm clears gate 1** (detection >= 0.70). C5a's false-flag rate
(0.10) is inside the 0.15 ceiling but its detection (0.5741) is not; C5b clears false-flag
(0.00) but not detection (0.6333) either. Gate 2 and gate 3 were not checked for either arm
since gate 1 already fails and s5.4's rule requires all three.

That makes six of eight arms scored (C1, C3, C2p, C7, C5a, C5b) and **C7 is still the only one
to clear all three gates.** Running gate-1 tally: C1 0.6111, C3 0.6037, C2p 0.0074 (fails by
design, the no-guardrail ablation), C7 0.7222 (passes), C5a 0.5741, C5b 0.6333 — none of the
5%-replay or reference-mixture variants scored so far reach the 0.70 floor except the raw-mixture
arm. Two arms remain: C4 and C6, in pass 3 (`ade4c590`), still running.

Promoted via `promote_scores.py acc01fdf --arms C5a,C5b` (dry-run verified first, no clashes).
Mirrored into `budget.json` (spend now 49.490 of 145) and this file. Dropped from the
pending-jobs marker.

## Pass 3 lands: C4 and C6, and the sweep is fully scored (2026-08-26 23:28Z)

`ade4c590-2e91-490d-8131-953015803a0c` (C4 + C6, the same L4 / 11 GB / full-item-count setup as
passes 1, 2 and 4) finished at 23:20:04Z after 2 h 40 m, `completion_status: success`, 2/2 arms
`rc=0`, 0 assertion failures, 30 per-arm artifacts plus `pack_summary.json` all present in the
job's artifact list. Billed 2.58 GPU-hours, in line with the 2.355--2.541 the other three
scoring passes cost.

C6 was the one unknown going in: a rank-64 adapter merges to the same 1.2B model as a rank-16
one, so its generation cost should be identical. It was --- 9,228 s against C4's 9,076 s, a 1.7%
difference on 1.32M and 1.34M generated tokens respectively. Rank does not survive the merge as
a serving cost, which is worth carrying into `s5.5`.

Item counts assert full on both arms: BFCL 3,490 items, IFStruct 2,000, IFEval 541, probes 434
graded and 30 clean. Peak GPU 10.56 GB an arm against the 11.0 GB ceiling, 22.0 GB concurrent on
a 23.7 GB card.

Scores read off the promoted `score.json` and then cross-checked field by field against the job
record's own `results` block --- the check that caught the pass-2 discrepancy. **The two agree
exactly on every field**, so there is nothing to withdraw here:

```
C4  bfcl 0.7128  ifstruct 0.1445  ifeval 0.7468  flag 0.6519  false 0.00
C6  bfcl 0.7237  ifstruct 0.1210  ifeval 0.7024  flag 0.6222  false 0.00
```

Promoted with `promote_scores.py ade4c590 --arms C4,C6` (dry run first, no clashes, so no
`--force` and nothing superseded). All eight arms now hold eight objects apiece under
`tidepool/s5.3/arms/<arm>/`.

### The whole sweep, on one table

Every number below is read from the arm's promoted `score.json`, not from any prose summary of
it. BFCL is the category-macro composite over both calling conventions; the flag and false-flag
columns are the 30-item synthetic clean arm, per the 08:34Z amendment.

| Arm | Delta from C1 | BFCL | IFStruct | IFEval | flag | false-flag | val_loss |
|---|---|---:|---:|---:|---:|---:|---:|
| C1 | reference | 0.7196 | 0.1375 | 0.7523 | 0.6111 | 0.00 | 0.1517 |
| C2p | guardrail_epochs 0 | 0.7070 | 0.1390 | 0.7283 | **0.0074** | 0.00 | 0.1511 |
| C3 | full-parameter, LR 1e-5 | 0.7110 | 0.1450 | 0.7837 | 0.6037 | 0.00 | 0.1513 |
| C4 | entropy-weighted loss | 0.7128 | 0.1445 | 0.7468 | 0.6519 | 0.00 | 0.1515 |
| C5a | 1% replay | 0.7013 | 0.1515 | 0.7837 | 0.5741 | 0.10 | 0.1517 |
| C5b | 5% replay | 0.7227 | 0.1095 | 0.7708 | 0.6333 | 0.00 | 0.1526 |
| C6 | LoRA r64 a128 | 0.7237 | 0.1210 | 0.7024 | 0.6222 | 0.00 | 0.1539 |
| **C7** | **raw mixture** | 0.7049 | 0.1400 | 0.7338 | **0.7222** | 0.00 | 0.1565 |

**Gate 1 (detection >= 0.70, false-flag <= 0.15), all eight arms: C7 passes, the other seven
fail.** The rescope trigger written into `s5.4` --- *every* arm failing gate 1 --- does not
fire. One arm clears it, so `s5.5` has a direction and the guardrail axis is reachable from
this training set.

Three things this table says that the four-arm view could not:

1. **Task quality is flat across the sweep and the guardrail axis is not.** BFCL spans 0.0224
   across eight arms (0.7013 to 0.7237) and IFEval spans 0.0813; the flag rate spans 0.7148,
   from C2p's 0.0074 to C7's 0.7222. Everything that separates these recipes lives on the axis
   the sweep was built to measure, and nothing separates them on the axes the field usually
   reports.
2. **The two highest BFCL scores belong to arms that fail gate 1.** C6 (0.7237) and C5b
   (0.7227) top the tool-calling column and detect at 0.6222 and 0.6333. Picking on the
   headline metric would have picked against the project's stated first priority, which is why
   the gates were pre-registered ahead of the gate-ranking metric.
3. **Validation loss points the wrong way at both ends.** Order the eight arms by val_loss
   and the best of them, C2p at 0.1511, posts the lowest detection rate in the sweep (0.0074);
   the worst, C7 at 0.1565, posts the highest (0.7222) and is the only arm to clear the gates.
   The six arms in between sit inside 0.0028 of each other on val_loss while their detection
   rates range over 0.0782, so the column does not separate them at all. No coefficient is
   claimed here --- the paired statistics come from the comparison job --- but the two
   endpoints are enough to say that an arm chosen on validation loss would have been the wrong
   arm, which is a finding about the sweep and belongs in the paper as one.

C5a is the only arm in the sweep to raise a false alarm at all (0.10, three of thirty). It is
inside the 0.15 ceiling, but on thirty items the ceiling is priced to about one item in thirty,
which is exactly the imprecision the probes pass below exists to remove.

### The probes pass is queued over all eight arms

Both cards went straight back out at 23:27Z, four arms apiece at 5.5 GB each, via
`queue_arms_probes.sh` so the two jobs are byte-identical in settings:

- `9ba23f0c-5deb-4168-a39a-6bb792e7d0bb` --- C1, C2p, C3, C4
- `2c91ab0d-a561-4fa8-bbd5-5e3a79278417` --- C5a, C5b, C6, C7

Probes only, `clean_control_object=tidepool/s5.3/tooldata/clean_control.jsonl`, full item
counts. This is the 138-item corpus clean arm the four scoring passes could not carry, because
`pack.yaml` leaves `clean_control_object` empty on purpose and no arm queue script set it. All
eight arms were scored identically without it, so it is added to all eight at once here rather
than to the later passes only --- otherwise C1 would have no corpus cell for any arm to pair
against. **The `s5.4` false-flag reading and the rescope trigger are read against these 138
items, not against the thirty.**

On promotion these supersede each arm's `scored_probes.jsonl` (same graded and synthetic-clean
items, plus the corpus arm, same model under greedy decoding), so it is
`--include scored_probes.jsonl --force` and nothing else --- the full pass's `score.json` stays
untouched, since a probes-only `score.json` carries no tool-calling, structured-output or
instruction-following rates. The superseded bytes are kept alongside, and joining the two files
on `id` over the shared items is a free determinism check that is now possible to run.

After these land: the comparison job over all eight arms in one call (Holm across the whole
family, so it cannot be split), then `s5.4`.

Spend 52.070 of 145.

---

## The probes pass landed, and the reproducibility check it made possible

Both jobs finished clean: `9ba23f0c-5deb-4168-a39a-6bb792e7d0bb` (C1, C2p, C3, C4) and
`2c91ab0d-a561-4fa8-bbd5-5e3a79278417` (C5a, C5b, C6, C7). Four arms a card, rc=0 on all
eight, 28 artifacts verified, 0 assertion failures, 0.123 and 0.130 GPU-h on `L4:1` at aws.
Each arm now carries 602 probe rows against 464 before: 434 graded, 30 synthetic clean, and
the 138-item corpus clean arm the four earlier scoring passes could not score against.

Before overwriting anything, every arm's old promoted `scored_probes.jsonl` was pulled down
and joined against the new one on `id`. **464 shared items per arm, zero verdict changes on
every one of the eight arms**, 138 items added. The two runs happened on different machines,
weeks apart in wall-clock terms and under a rebuilt pack script, so the join is a real
end-to-end reproducibility result and not a tautology: same adapter, same greedy decode, same
grader, byte-identical verdicts on every item both passes share. Nothing in the project had
demonstrated that before, and it is worth a line in the paper.

Promotion was `--include scored_probes.jsonl --force` and nothing else. The default include
globs would have carried `score.json` and `eval_summary.json` across too, and a probes-only
summary has no BFCL, structured-output or instruction-following block in it, so the full
pass's summaries would have been replaced by ones missing three quarters of the sweep. The
superseded bytes are kept alongside as `scored_probes.jsonl.superseded-by-<job>`.

### Gate 1 on the 138-item corpus clean arm

The pre-registered bar is detection at or above 0.70 with a false-alarm rate at or below
0.15. Read against the corpus arm rather than the thirty synthetic items:

| arm | detection | false alarm (138 corpus) | false alarm (all 168 clean) | gate 1 |
|-----|-----------|--------------------------|------------------------------|--------|
| C1  | 0.6111 | 0.0145 | 0.0119 | fails detection |
| C2p | 0.0074 | 0.0000 | 0.0000 | fails detection |
| C3  | 0.6037 | 0.0217 | 0.0179 | fails detection |
| C4  | 0.6519 | 0.0290 | 0.0238 | fails detection |
| C5a | 0.5741 | 0.0217 | 0.0357 | fails detection |
| C5b | 0.6333 | 0.0000 | 0.0000 | fails detection |
| C6  | 0.6222 | 0.0000 | 0.0000 | fails detection |
| **C7** | **0.7222** | **0.0217** | **0.0179** | **passes** |

**C7 is the only arm to clear gate 1, and it clears it on detection with the false-alarm
side barely used**: three flagged returns out of 138 clean ones, against a ceiling of about
twenty. Every failing arm fails on detection alone. No arm is anywhere near the false-alarm
ceiling, which retires the worry the thirty-item arm raised: C5a's 0.10 there was three items
out of thirty, and on 138 items the same arm reads 0.0217.

The pre-registered rescope trigger fires only if **every** arm fails gate 1. One arm passes,
so it does not fire and `s5.5` has a direction to take.

## The comparison, and three amendments made to it after the rates were known

The paired comparison over all eight arms in one call is `s5-compare`, task
`ecd12a36-da98-4a18-bc38-2889905746e9`, CPU only and no GPU-hours. It ran twice. The first
run, `08ab59c0-e8fc-4f9e-87ae-159d44579ab0`, is superseded and its numbers are not cited
anywhere; it is recorded here because what it caught is the point.

**Amendment 1, made before the first run: the gate itself had no paired test.** The shipped
component table paired on `correct`, which on a probe row means "did not invent a value" on
the malformed arms and "did not flag, and used the value" on the clean ones. The `s5.4`
reliability gate is not written on that field. It is a detection rate and a false-alarm rate,
and both are `detail.flagged`. The two readings disagree by more than twenty points on the
same items: C1 scores 0.9259 on `correct` and 0.6111 on the flag rate over the same 270
malformed probes. So the comparison would have tested everything except the axis the decision
turns on. Two cells were added, `probes_flag_detect` and `probes_flag_clean_corpus`, the
second inverted because on a clean return a raised flag is the error. The amendment enlarges
the Holm family by seven arms times two cells, which makes every claim in the run harder to
sustain rather than easier, and it was made without first computing whether it changes any
outcome.

**Amendment 2, made after the first run reported sixteen assertion failures: BFCL v3 ships a
duplicated item id.** Two different `live_relevance` questions both carry the id
`live_relevance_3-3-0`, and the model answers them differently, so they are genuinely two
items under one label. Keying on the bare id kept whichever row came last and dropped the
other, in every arm and both calling styles: 3,490 rows joined as 3,489 items. `read_items`
now keeps repeats as `<id>#2` and pairs the kth occurrence in one arm against the kth in
another. Occurrence pairing is only exact if both arms walked the corpus in the same order,
so that is now asserted per arm and component against the reference's id sequence rather than
assumed, which doubles as a free check that no arm was scored over a different corpus.

**Amendment 3, made after the first run reported sixteen un-cross-checked cells: the
cross-check was aimed at the wrong file.** Both BFCL cells named
`bfcl.styles.<style>.item_weighted`, and `score.json` is a flat card that carries a macro
composite and nothing per-style. The per-style item-weighted rate lives in
`eval_summary.json` under `results.bfcl.styles.<style>.item_weighted`. So the check silently
resolved to nothing on all sixteen arm-by-style cells and the comparison's central component
was joining several thousand items with no independent number to reconcile against. The
driver now loads both summaries and the check paths name which one they mean. The two defects
compound: the cross-check that was switched off is exactly the one that would have caught the
duplicate id, since 3,489 joined items against a reported rate over 3,490 is a mismatch.

Neither of the two amendments is a judgement call about the result. Both were forced by the
first run's own assertions and notes, which is what those assertions are for; the honest
reading is that the first run was a test of the harness that the harness failed, and it
failed loudly rather than quietly. Driver checks: 51, none failing.

## The comparison: seventy cells, nine survive, and eight of the nine are losses

`3fec1ed8-ef99-4fbf-a7ab-1ec941c27284`, all eight arms against C1 in one call, ten components
an arm, CPU only, no GPU-hours, **0 assertion failures**. Every arm's joined rate now
reconciles with the rate its own `eval_summary.json` reports, which is the check that had
been silently switched off. Artifacts `comparison.json` and `comparison.md` are mirrored into
`runs/s5.3-compare/`; the superseded first run is kept beside them under
`superseded-08ab59c0/` and is cited nowhere.

70 comparisons. 22 separate by interval. **9 survive Holm across the whole family:**

| arm | component | delta | 95% CI | Holm p |
|-----|-----------|-------|--------|--------|
| C2p | detection (`flagged`) | −0.6037 | [−0.6630, −0.5444] | 1.2e−47 |
| C2p | no-fabrication | −0.1444 | [−0.1852, −0.1037] | 2.5e−10 |
| C2p | BFCL tools-text | −0.0364 | [−0.0458, −0.0272] | 5.4e−13 |
| C2p | BFCL native | −0.0352 | [−0.0476, −0.0229] | 1.6e−06 |
| C7 | BFCL tools-text | −0.0287 | [−0.0381, −0.0189] | 6.8e−07 |
| C5b | IFStruct | −0.0280 | [−0.0435, −0.0120] | 3.5e−02 |
| C7 | BFCL native | −0.0255 | [−0.0358, −0.0152] | 1.2e−04 |
| C4 | BFCL native | −0.0146 | [−0.0229, −0.0063] | 4.1e−02 |
| **C7** | **detection (`flagged`)** | **+0.1111** | **[+0.0556, +0.1667]** | **1.1e−02** |

**One family-corrected gain exists in the entire sweep and it is C7's detection rate.** Every
other surviving cell is an arm doing worse than the reference. C3, C5a and C6 have no
surviving cell in either direction: on this evidence they are the reference recipe with a
knob turned and nothing to show for it.

**The amendment that added the flag cells is what made the finding sayable.** On `correct`,
C7's probe delta is +0.0111 with a Holm p of 1.0, indistinguishable from C1. On
`detail.flagged`, the same 270 items give +0.1111 at Holm p = 0.011. The two readings of one
file disagree about whether the guardrail axis moved at all, and only one of them is the
axis the gate is written on. Had the family stayed at 56 cells, C7's detection cell would
have been claimed on a smaller family and would still have survived; enlarging the family
made the claim harder and it held.

**C2p is the cleanest ablation result in the project.** Dropping the guardrail block costs
60.4 points of detection with an interval that excludes zero by ten standard errors, and it
costs 3.5 points of tool calling as well. The guardrail data is not a tax on tool calling.
It is doing the detection work, and removing it takes tool calling down with it.

**C7's trade is real in both directions.** +11.1 points of detection against −2.87 and −2.55
on the two calling conventions, all three family-corrected. No arm in the sweep buys
detection for free, and the sweep's job was to find out whether one could.

**The clean arms stayed still.** No arm has a surviving cell on `probes_flag_clean_corpus`,
`probes_clean_corpus`, `probes_clean` or `probes_stack_idiom`. The detection gains and losses
are not arms flagging more often across the board; C7 flags 11 points more on malformed
returns and 0.7 points *less* on clean ones.

## Gate 2 was read wrong at 13:15Z, and C7 does not pass it

The 13:15Z entry recorded that C7 "passes gate 2 (no category more than 3.0 points below C1
on bfcl, within 2.0 on ifeval)". That reading compared composites. Gate 2 is a
**worst-category** floor, and read the way it is written, on the per-category table in each
arm's `eval_summary.json`, C7 fails it. On its own better convention, tools-text:

| category | n | C1 | C7 | delta (points) |
|----------|---|----|----|----------------|
| live_irrelevance | 882 | 0.5329 | 0.4649 | **−6.80** |
| live_simple | 258 | 0.6512 | 0.6318 | −1.94 |
| live_multiple | 1052 | 0.6492 | 0.6302 | −1.90 |
| simple | 400 | 0.9175 | 0.9025 | −1.50 |
| irrelevance | 240 | 0.6083 | 0.5833 | −2.50 |
| parallel | 200 | 0.8100 | 0.8000 | −1.00 |
| multiple | 200 | 0.8700 | 0.8650 | −0.50 |
| live_parallel, live_parallel_multiple, live_relevance, parallel_multiple | 258 | — | — | 0.00 |

**`live_irrelevance` is 6.80 points down, and it is the largest category in the benchmark at
882 items.** On native-tools the picture is worse: five categories more than 3.0 below, led
by `parallel_multiple` at −8.00 and `live_irrelevance` at −6.46. The direction is coherent
rather than noisy, and it is the direction the detection result predicts. `irrelevance` and
`live_irrelevance` are the categories that reward *declining* to call a tool; a recipe
trained to be more suspicious of tool returns is also, apparently, more willing to call a
tool it should have refused. The guardrail axis and the abstention axis are not independent,
and nothing in the design anticipated that.

A second correction from the same check. The 13:15Z entry recorded C7's IFEval as 0.7411. The
arm's own `score.json` and `eval_summary.json` both say **0.7338**, and the comparison job
agrees. Gate 2's IFEval clause allows 2.0 points below; the true gap to C1 is 1.85, not the
1.12 the wrong figure implied. C7 passes that clause, with far less room than recorded.

Gate 3 is unaffected: C7's IFStruct is 0.1400 against C1's 0.1375, at or above, so it passes.

**So the tally is: C7 passes gate 1 and gate 3 and fails gate 2, and no other arm passes gate
1. No arm in the sweep passes all three gates.** The pre-registered rule's tie-break picks
"the cheapest recipe that passes all three gates" and its rescope trigger fires only when
every arm fails gate 1. Neither clause covers this outcome, which is a gap in a rule written
before the numbers, and s5.4 has to close it rather than pretend one of the two clauses fits.

### The gate is written against the matched base, and the matched base had no per-category table

Gate 2 says "more than 3.0 points below **the matched base**", not below C1. The table above
uses C1, because that is what the 13:15Z entry did and the correction has to be comparable to
what it corrects. Read literally the gate needs B1, and B1's per-item files and per-category
BFCL table were lost to the typed-save bug, so **gate 2 has been unevaluable as written for
the whole stage** and nobody noticed, because the composite reading it was replaced with
happened to pass.

One earlier framing needs correcting too. This log recorded B1's scored files as
"unretrievable". They are listed in B1's job record under `eval_results`, so they were saved;
what is true is that no command available here can fetch them, since `job download` reaches
artifacts only. The distinction matters because it changes the fix from "regenerate the
numbers" to "get at numbers that exist", and the cheapest route to either is the same one:
the score-only replay the harness already has.

`f611bb2b-e847-4323-85a9-6c932d826387`, queued at 00:21Z: `s5-eval` with
`rescore_object=tidepool/s5.2/B1-fp-instruct`, full profile, no weights loaded and nothing
generated, the same graders the eight arms were scored by. About 0.2 GPU-h on an idle L4.
Its output goes to `tidepool/s5.3/arms/B1/`, which does two things at once: gate 2 becomes
evaluable against the base it was written against, and **B1 enters the paired comparison as
an arm**, so the plan's headline claim of +3.0 BFCL over a matched base rerun finally gets a
test instead of a pair of composites subtracted from each other.


## The matched base comes back, and it takes gate 2 down with it

Gate 2 is written against B1, the matched base, on both halves: no BFCL v3 category more
than 3.0 below it, and IFEval no more than 2.0 below it. B1's per-category table was lost
to the typed-save bug, so for the whole of s5.3 the gate had been checked against C1
instead. C1 is the reference arm, not the base. Checking a floor written against an
untuned model by comparing tuned arms to each other measures the wrong thing, and it
measured it in the flattering direction.

The score-only replay `f611bb2b` recovered the table. It points the s5 eval task at
`tidepool/s5.2/B1-fp-instruct` through `rescore_object`, so it loads no weights and
generates nothing: it joins the saved completions on id, re-renders every prompt, checks
the recorded hashes, and runs the current graders over them. 9,985 completions replayed,
four components, zero assertion failures, `generated_tokens: 0`, 4m25s on one L4.

Two things came out of it before the gate was even read.

The replayed B1 does not match the B1 row recorded at s5.2. Composite 0.6649 against
0.6700, IFEval 0.8189 against 0.8170. The graders moved after s5.2 was written, and the
eight arms were all scored by the current ones. The replayed figures are the correct
comparable base; the s5.2 row is a measurement taken with a different instrument and
should not be differenced against anything in this stage.

B1's own logs confirm the duplicate-id defect the comparison harness was fixed for, from
a completely independent direction: `3490 rows (3489 unique ids)` and `1 prompt-hash
mismatches` on both BFCL styles. Two `live_relevance` questions share the id
`live_relevance_3-3-0` and differ in text, which is why the hash check fires on exactly
one item.

### The BFCL half: every arm clears it, and clears the plan's headline claim

Best-convention composite, against the replayed base:

| arm | best style | BFCL composite | vs B1 |
|-----|-----------|---------------:|------:|
| B1  | native_tools | 0.6649 | — |
| C1  | tools_text | 0.7196 | +5.47 |
| C2p | tools_text | 0.7070 | +4.21 |
| C3  | tools_text | 0.7110 | +4.61 |
| C4  | tools_text | 0.7128 | +4.79 |
| C5a | tools_text | 0.7013 | +3.64 |
| C5b | tools_text | 0.7227 | +5.78 |
| C6  | native_tools | 0.7237 | +5.88 |
| C7  | tools_text | 0.7049 | +4.00 |

The plan's `+3.0 over matched base` target on tool calling is met by all eight arms, with
margin. Note also that the tuning flipped which calling convention each model is better
at: B1 and C6 do better with native tool syntax, every other arm does better with tools
described in text.

### The IFEval half: every arm fails it

| arm | prompt_strict | vs B1 | prompt_loose | instr_strict | instr_loose |
|-----|-------------:|------:|-------------:|-------------:|------------:|
| B1  | 0.8189 | — | 0.8299 | 0.8657 | 0.8765 |
| C1  | 0.7523 | −6.65 | −6.10 | −4.08 | −3.84 |
| C2p | 0.7283 | −9.06 | −7.58 | −5.76 | −4.92 |
| C3  | 0.7837 | −3.51 | −3.33 | −2.04 | −1.92 |
| C4  | 0.7468 | −7.21 | −6.10 | −4.92 | −4.20 |
| C5a | 0.7837 | −3.51 | −3.33 | −2.04 | −2.04 |
| C5b | 0.7708 | −4.81 | −4.62 | −3.12 | −3.00 |
| C6  | 0.7024 | −11.65 | −10.91 | −7.67 | −7.19 |
| C7  | 0.7338 | −8.50 | −8.32 | −5.04 | −4.92 |

On the convention this project has used throughout, `prompt_level_strict`, every arm is
between 3.5 and 11.7 points below the base against a 2.0 allowance. The result survives
every other IFEval convention too: the single cell anywhere in the table that comes in
under 2.0 is C3 on `instruction_level_loose` at −1.92, and C3 fails gate 1 anyway. The
gap recorded earlier as `C7 is 1.85 below, inside the allowance` was C7 against C1, and
is void.

So the whole sweep bought tool calling and paid for it in instruction following. Both
halves of that trade are large, both are consistent across all eight arms, and neither
was visible while the gate was being read against the reference arm.

### The per-category half, for completeness

Read within `tools_text`, the better convention for seven of the eight arms, C7 breaches
the 3.0 floor on exactly one category (`live_irrelevance`, −9.07) and C1 breaches none;
its worst is `live_irrelevance` at −2.27. Read within `native_tools`, B1's own better
convention, C7 breaches five categories and C1 breaches four (live_parallel −6.25,
live_relevance −5.56, multiple −3.50, simple −6.75).

Two of C1's four native breaches are on categories of 16 and 18 items, where one item is
6.25 and 5.56 points. A fixed 3.0-point floor applied across categories spanning 16 to
1,052 items is a floor of half an item at one end and 32 items at the other. Whichever
convention is chosen, the floor as written is not measuring the same thing in every cell,
and s5.4 should say so rather than apply it and move on.

None of this changes the conclusion the IFEval half already forces: **no arm passes gate
2, and therefore no arm passes all three gates.** The per-category question is now about
how to write the gate for s5.5, not about which arm survives s5.3.

## The base joins the family, and the one gain survives it

`c1add251` reran the paired comparison with B1 as a ninth arm, reference still C1. 78
cells, 28 separate by interval, 14 survive Holm across the whole arm-by-component family.

Adding the base did not cost the sweep's only gain anything. C7 `probes_flag_detect`
moves from Holm p 1.13e−2 in the eight-arm family to 1.18e−2 in the nine-arm one:
+0.1111 [+0.0556, +0.1667] on 270 paired items either way. Holm behaves that way when the
added cells are more significant than the one in question, since they sit ahead of it in
the ordering and its rank multiplier is unchanged.

What B1 buys is a paired test behind the claim the plan opened with. Reference against
matched base, item-weighted, on the same items:

| component | C1 minus B1 | 95% CI | Holm p | n |
|-----------|-----------:|:------:|-------:|--:|
| BFCL native tools | +3.15 pts | [+1.78, +4.53] | 6.4e−4 | 3490 |
| BFCL tools-in-text | +12.38 pts | [+10.63, +14.13] | 3.2e−42 | 3490 |
| probe detection (`correct`) | +11.85 pts | [+8.15, +15.56] | 3.4e−8 | 270 |
| probe detection (`flagged`) | +60.37 pts | [+54.44, +66.30] | 1.3e−47 | 270 |
| IFStruct | +0.20 pts | [−1.50, +1.90] | 1.0 | 2000 |
| IFEval | **−6.65 pts** | [−9.80, −3.51] | 4.8e−3 | 541 |
| probes, synthetic clean | +26.67 pts | [+10.00, +43.33] | 0.49 | 30 |
| probes, stack idiom | −5.56 pts | [−12.50, +1.39] | 1.0 | 144 |

The tool-calling win over the base is real and family-corrected, on both conventions. So
is the instruction-following loss. The summary-number gap of 6.65 points holds up as a
paired, per-item, family-corrected result, which removes the last way of reading it as an
artifact of the base's own scoring pass.

One incidental result worth keeping. B1's flag rate against C1 is −0.6037 with interval
[−0.6630, −0.5444], which is C2p's number to four decimal places and the same interval.
The untuned base and the arm trained with the guardrail slice removed flag at
indistinguishable rates. Whatever produces the flagging behaviour is that slice
specifically, not fine-tuning in general and not the rest of the mixture.

### Two assertion failures, both an absence rather than a fault

The run reported two: `B1 and C1 read probes_clean_corpus in the same item order` and the
same for `probes_flag_clean_corpus`, with B1 hashing to `da39a3ee5e6b`. That is the sha of
the empty sequence. B1 was scored without a `clean_control_object`, so it selects nothing
for the corpus components while every tuned arm selects 138 rows, and an empty sha never
equals a non-empty one. Both cells were already dropped for B1 with a note, so nothing in
the reported numbers is affected.

The order check now skips a component when either side selected no rows, with a regression
test that fails against the old code (`one arm missing a component is not an
order-assertion failure`). The suite is at 53 checks, 0 failed. The recorded output of
`c1add251` still carries the two failures, which is correct: the fix came after it, and
overwriting the record to make it look clean would be worse than explaining it.
