"""Probe B — the technologies, not the vendor SDK.

The operator's standing instruction is to specialize the fleet to the underlying
technologies the agent stack sits on, and to name them: SQL dialects, JSON schemas, MCP,
IPC, containers, tracing semantics. A model that has memorized one vendor's client library
scores well on ordinary code benchmarks and still gets these wrong, because each item turns
on a specification detail that differs between two things a careless answer treats as the
same. That is the point of the set: **every item has a near neighbour that would be right
under a different dialect, version, or transport**, so answering from a general impression
of "how this kind of thing works" fails it.

Grading is by regex over the answer: `must` patterns all have to appear, `must_not`
patterns none of them. Each item states, in `trap`, which wrong answer the `must_not` list
is there to catch, so a failing model's error is legible rather than just scored.
"""

FAMILIES = [
    # ------------------------------------------------------------------ SQL dialects
    {"family": "sql_dialect", "items": [
        {"q": "In PostgreSQL, write a query returning `first_name || ' ' || last_name` as `full_name` from `people`. Use the operator Postgres provides for string concatenation, not a function.",
         "must": [r"\|\|", r"(?i)as\s+full_name"], "must_not": [r"(?i)\bCONCAT\s*\("],
         "trap": "MySQL's CONCAT(), which Postgres also accepts, so a model that reaches for it never learns the operator"},
        {"q": "In MySQL 8, concatenate `first_name`, a space, and `last_name` as `full_name` from `people`. MySQL's `||` is logical OR by default, so do not use it.",
         "must": [r"(?i)CONCAT\s*\("], "must_not": [r"\|\|"],
         "trap": "carrying Postgres's || across, where it silently means OR unless PIPES_AS_CONCAT is set"},
        {"q": "In PostgreSQL, select rows from `city` where `name` matches 'porto' case-insensitively, using the operator Postgres has for exactly that. Do not lower() both sides.",
         "must": [r"(?i)\bILIKE\b"], "must_not": [r"(?i)lower\s*\(\s*name"],
         "trap": "lower(name) LIKE lower(...), which works but is not the dialect feature asked for"},
        {"q": "In SQLite, return the 10 rows after the first 20 from `events` ordered by `ts`. SQLite has no OFFSET-less TOP syntax.",
         "must": [r"(?i)LIMIT\s+10", r"(?i)OFFSET\s+20"], "must_not": [r"(?i)\bTOP\s+\d", r"(?i)FETCH\s+FIRST"],
         "trap": "SQL Server's TOP or the ANSI FETCH FIRST clause, neither of which SQLite parses"},
        {"q": "In SQL Server, return the first 10 rows of `events` ordered by `ts` descending, using the T-SQL keyword rather than LIMIT.",
         "must": [r"(?i)\bTOP\s*\(?\s*10"], "must_not": [r"(?i)\bLIMIT\b"],
         "trap": "LIMIT, which SQL Server does not accept at all"},
        {"q": "In PostgreSQL, insert a row into `orders` and get the generated `id` back in the same statement, without a second SELECT.",
         "must": [r"(?i)RETURNING\s+id"], "must_not": [r"(?i)LAST_INSERT_ID", r"(?i)SCOPE_IDENTITY"],
         "trap": "MySQL's LAST_INSERT_ID() or T-SQL's SCOPE_IDENTITY(), which are the same idea in the wrong dialect"},
        {"q": "In BigQuery standard SQL, count distinct `user_id` in `events` for the last 7 days using the date function BigQuery provides for interval arithmetic on a DATE column named `d`.",
         "must": [r"(?i)DATE_SUB\s*\(", r"(?i)INTERVAL\s+7\s+DAY"], "must_not": [r"(?i)DATEADD\s*\(", r"(?i)d\s*-\s*7"],
         "trap": "T-SQL's DATEADD, or bare date minus integer, which is Postgres and not BigQuery"},
        {"q": "In PostgreSQL, extract the `city` key from a `jsonb` column `addr` as text, using the operator that returns text rather than jsonb.",
         "must": [r"->>"], "must_not": [r"(?i)JSON_EXTRACT\s*\("],
         "trap": "MySQL's JSON_EXTRACT, and the ->  operator, which returns jsonb rather than text"},
        {"q": "In MySQL 8, extract the `city` key from a JSON column `addr` and return it unquoted as a string.",
         "must": [r"(?i)(JSON_UNQUOTE|->>)"], "must_not": [r"(?i)jsonb"],
         "trap": "reaching for Postgres jsonb operators on a MySQL JSON column"},
        {"q": "In PostgreSQL, upsert into `counts(k, n)` so a conflict on `k` adds to the existing `n`. Use the Postgres clause, not MERGE.",
         "must": [r"(?i)ON\s+CONFLICT", r"(?i)DO\s+UPDATE"], "must_not": [r"(?i)ON\s+DUPLICATE\s+KEY"],
         "trap": "MySQL's ON DUPLICATE KEY UPDATE"},
    ]},
    # ------------------------------------------------------------------- JSON Schema
    {"family": "json_schema", "items": [
        {"q": "Write a JSON Schema for draft 2020-12 describing an object with a required string `name` and an optional integer `age` at least 0. Declare the dialect explicitly.",
         "must": [r"2020-12", r'"\$schema"', r'"required"'], "must_not": [r'"definitions"'],
         "trap": "draft-07 furniture in a document that claims to be 2020-12"},
        {"q": "In JSON Schema 2020-12, define a reusable subschema and reference it. Use the keyword 2020-12 introduced, not the older one.",
         "must": [r'"\$defs"', r'"\$ref"'], "must_not": [r'"definitions"'],
         "trap": "definitions, which draft-07 used and 2020-12 replaced with $defs"},
        {"q": "In JSON Schema 2020-12, describe a tuple of exactly [string, integer] with no extra elements. Use the 2020-12 keyword for positional schemas.",
         "must": [r'"prefixItems"'], "must_not": [r'"items"\s*:\s*\['],
         "trap": "draft-07's array-form `items`, which 2020-12 reinterprets as applying to every element"},
        {"q": "Write a JSON Schema that forbids any property not listed in `properties`.",
         "must": [r'"additionalProperties"\s*:\s*false'], "must_not": [r'"additionalProperties"\s*:\s*true'],
         "trap": "assuming closed-world by default; JSON Schema objects are open unless told otherwise"},
        {"q": "In JSON Schema, express that a value must be either null or a string. Do not use a `type` of \"any\".",
         "must": [r'"type"\s*:\s*\[', r'"null"'], "must_not": [r'"any"'],
         "trap": "the `any` type, which is not a JSON Schema type and comes from other schema languages"},
        {"q": "Explain what `required: [\"a\"]` does in JSON Schema when the instance is not an object.",
         "must": [r"(?i)(not an object|non-object|only applies|no effect|ignored|vacuous)"],
         "must_not": [r"(?i)always fails"],
         "trap": "assuming required fails on a non-object; object keywords are vacuously true off-type"},
        {"q": "In an OpenAI-style tool definition, what must the top-level `parameters` value be, and what happens if you put the property map directly under it?",
         "must": [r'(?i)"?type"?\s*:?\s*"?object', r"(?i)properties"],
         "must_not": [r"(?i)(either is fine|both work|does not matter)"],
         "trap": "the common malformed shape where properties are inlined under parameters with no type or properties key"},
    ]},
    # --------------------------------------------------------------------------- MCP
    {"family": "mcp", "items": [
        {"q": "Write the JSON-RPC request an MCP client sends to invoke a tool named `search` with argument `q`. Include every field the JSON-RPC 2.0 envelope requires.",
         "must": [r'"jsonrpc"\s*:\s*"2\.0"', r'"method"\s*:\s*"tools/call"', r'"id"', r'"name"\s*:\s*"search"'],
         "must_not": [r'"method"\s*:\s*"call_tool"'],
         "trap": "inventing a method name; MCP's is tools/call and the envelope is plain JSON-RPC 2.0"},
        {"q": "In MCP, what method does a client call to discover which tools a server exposes, and what shape comes back?",
         "must": [r"tools/list", r"(?i)(inputSchema|input_schema|schema)"], "must_not": [r"(?i)GET\s+/tools"],
         "trap": "treating MCP as REST; it is JSON-RPC over a transport, not HTTP paths"},
        {"q": "An MCP tool call fails because the argument was the wrong type. Is that a JSON-RPC error object or a result with an error flag, and what distinguishes the two cases?",
         "must": [r"(?i)isError|is_error|content", r"(?i)(protocol|invalid params|-32602|transport)"],
         "must_not": [r"(?i)there is no difference"],
         "trap": "collapsing protocol-level JSON-RPC errors and tool-level failures reported inside a successful result"},
        {"q": "In JSON-RPC 2.0 as MCP uses it, what makes a message a notification rather than a request, and may a server respond to one?",
         "must": [r"(?i)no\s+`?id`?|omits the id|without an id", r"(?i)(must not|no response|not respond)"],
         "must_not": [r"(?i)notifications are acknowledged"],
         "trap": "assuming every message gets a reply; a notification has no id and must not be answered"},
        {"q": "An MCP client sends three tool calls concurrently over one stdio transport. What correlates each response with its request?",
         "must": [r"(?i)\bid\b"], "must_not": [r"(?i)(order|first-in|sequence|FIFO)"],
         "trap": "assuming responses arrive in request order; JSON-RPC correlates by id and permits reordering"},
    ]},
    # --------------------------------------------------------------------------- IPC
    {"family": "ipc", "items": [
        {"q": "A parent writes to a child over a pipe and the child exits early. What does the next write do by default, and how do you handle it rather than dying?",
         "must": [r"(?i)SIGPIPE", r"(?i)(EPIPE|ignore|SIG_IGN|handler)"], "must_not": [r"(?i)returns\s+0\b"],
         "trap": "assuming a short write; the default disposition kills the writer before it sees EPIPE"},
        {"q": "Why does a length-prefixed frame beat a newline delimiter for sending arbitrary binary payloads over a unix socket?",
         "must": [r"(?i)(binary|arbitrary|contain).{0,40}(newline|delimiter|\\n)|escap"],
         "must_not": [r"(?i)(faster|performance) is the main"],
         "trap": "arguing from speed; the reason is that the payload may contain the delimiter"},
        {"q": "Two processes need to pass an open file descriptor between them. Which IPC mechanism can do that, and which cannot?",
         "must": [r"(?i)(unix domain|AF_UNIX|SCM_RIGHTS)", r"(?i)(TCP|pipe|cannot|not possible)"],
         "must_not": [r"(?i)any socket can"],
         "trap": "assuming all sockets are alike; fd passing needs AF_UNIX with SCM_RIGHTS"},
        {"q": "A reader does a single read() of 4096 bytes on a stream socket and gets 1200. Is the message truncated?",
         "must": [r"(?i)(no|not necessarily|partial|short read)", r"(?i)(loop|until|more)"],
         "must_not": [r"(?i)yes, (it is )?truncated"],
         "trap": "treating a stream socket as message-oriented; boundaries are not preserved"},
    ]},
    # -------------------------------------------------------------------- containers
    {"family": "containers", "items": [
        {"q": "In a Dockerfile, what is the difference between ENTRYPOINT and CMD when both are set, and what does `docker run img foo` do?",
         "must": [r"(?i)(CMD).{0,80}(argument|default|override)", r"(?i)foo"],
         "must_not": [r"(?i)CMD replaces ENTRYPOINT"],
         "trap": "believing run arguments replace ENTRYPOINT; they replace CMD and become its arguments"},
        {"q": "A container's process ignores SIGTERM on `docker stop`. Explain the shell-form trap and how exec form fixes it.",
         "must": [r"(?i)(shell form|/bin/sh -c|sh -c)", r"(?i)(exec form|JSON array|exec )", r"(?i)PID\s*1"],
         "must_not": [r"(?i)(SIGKILL is the only|nothing you can do)"],
         "trap": "missing that shell form puts /bin/sh at PID 1, which does not forward signals to its child"},
        {"q": "Why does `COPY . .` before `RUN pip install -r requirements.txt` make rebuilds slower than the other order?",
         "must": [r"(?i)(cache|layer)", r"(?i)(invalidat|bust|miss)"], "must_not": [r"(?i)(image size|smaller image) is the reason"],
         "trap": "answering about image size; the cost here is layer cache invalidation on every source edit"},
        {"q": "What does the OCI image spec say a container's `WORKDIR` does if the directory does not exist, in the Docker builder?",
         "must": [r"(?i)(creat|made|mkdir)"], "must_not": [r"(?i)(error|fails|must exist)"],
         "trap": "assuming it errors; the builder creates it"},
        {"q": "A pod's readiness probe passes but the service sends no traffic. Name two causes that are about label and port wiring rather than about the probe.",
         "must": [r"(?i)(selector|label)", r"(?i)(targetPort|containerPort|named port|port)"],
         "must_not": [r"(?i)the probe is wrong"],
         "trap": "re-diagnosing the probe when the question rules that out"},
    ]},
    # ----------------------------------------------------------------------- tracing
    {"family": "tracing", "items": [
        {"q": "Give the W3C `traceparent` header for trace id 4bf92f3577b34da6a3ce929d0e0e4736, span id 00f067aa0ba902b7, sampled. State the version field.",
         "must": [r"00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01"],
         "must_not": [r"(?i)X-B3-TraceId"],
         "trap": "answering in B3 headers, which are Zipkin's and a different format"},
        {"q": "In OpenTelemetry, a service receives a request with a traceparent and starts work. Does the new span reuse the incoming span id or the incoming trace id?",
         "must": [r"(?i)trace\s*id.{0,60}(same|reuse|propagat|inherit)", r"(?i)(new|fresh|its own)\s+span\s*id"],
         "must_not": [r"(?i)(both are new|new trace id)"],
         "trap": "minting a new trace id per service, which breaks the trace into disconnected pieces"},
        {"q": "What is the difference between an OpenTelemetry span attribute and a span event, and when does a log line belong as an event rather than an attribute?",
         "must": [r"(?i)(timestamp|point in time|moment)", r"(?i)(key.value|attribute).{0,60}(whole|entire|span)"],
         "must_not": [r"(?i)(they are the same|interchangeable)"],
         "trap": "treating events as free-form attributes; an event carries its own timestamp within the span"},
        {"q": "A trace shows a 900ms parent span with two 100ms children and no other spans. What can you conclude about the missing 700ms, and what can you not?",
         "must": [r"(?i)(uninstrument|not instrumented|no span|gap)"],
         "must_not": [r"(?i)(the parent is slow because|proves)"],
         "trap": "reading unattributed time as parent-local work rather than as missing instrumentation"},
        {"q": "In OpenTelemetry, what does a sampling decision of `RECORD_ONLY` mean for whether a span is exported?",
         "must": [r"(?i)(not export|no export|recorded but not)"], "must_not": [r"(?i)(exported|sent to the backend)\s*$"],
         "trap": "conflating recording with exporting; RECORD_ONLY records and does not export"},
    ]},
]
